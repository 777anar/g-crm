# G-ERP — API Specification

_Originally a Phase 1 design document (2026-06-29); rewritten 2026-07-21 against the actual current source code (every FastAPI router in `backend/`, not the original design) to close `PROJECT_AUDIT.md` Priority #4 — the previous revision stopped documenting real endpoints after Version 2.12 and still described a superseded Phase-1 CRM/Sales design that was never actually built that way._
_Status: covers all ten installed modules (Core, CRM, Stone Catalog, Sales, Orders, Production, Installation, Finance, Communication Center + Real Integrations, AI Sales Assistant, Reports) as implemented as of Version 2.28.0. Marketing and Purchasing are not built — see `PROJECT_AUDIT.md` §3._

This specification defines the conventions every module's API follows, plus the full, current contract for every endpoint in the system. OpenAPI schema is auto-generated by FastAPI (`/api/v1/openapi.json`, interactive docs at `/api/v1/docs`) and is the always-current source of exact request/response shapes; this document is the human-readable map of what exists and why, verified against the router/schema source files, not hand-maintained from memory.

---

## 1. Versioning

- All endpoints are prefixed `/api/v1/...`. The version is a URL path segment, not a header, for discoverability and simple routing in FastAPI.
- Each module mounts under `/api/v1/<module_name>/...` per its `MODULE_MANIFEST` (`core/module_registry/registry.py`): `app.include_router(manifest.router, prefix=f"/api/v1/{manifest.name}")`. Module names today: `crm`, `catalog`, `sales`, `orders`, `production`, `installation`, `finance`, `communication`, `ai`, `reports`. Core's own endpoints (auth, companies, documents, events) mount directly under `/api/v1/core/...` or `/api/v1/auth/...`, outside the module-manifest system.
- A breaking change to any endpoint's request/response shape would require a new version prefix (`/api/v2/...`) for that module's affected routes — additive, backward-compatible changes (new optional fields, new endpoints) do not. No `/api/v2/...` route exists anywhere yet.

## 2. Authentication

- **Mechanism**: JWT bearer tokens. `Authorization: Bearer <access_token>` header on every authenticated request. No server-side session/cookie-only auth path — mobile clients would work identically to web.
- **Token types**:
  - **Access token**: short-lived (15 minutes), carries `user_id`, `active_company_id`, `role`, `module_permissions` claims.
  - **Refresh token**: long-lived (30 days), stamped with a `gen` (generation) claim used for revocation (see `/auth/logout` below). Stored client-side (web: `localStorage`, per `frontend/lib/session.ts` — see `PROJECT_AUDIT.md` §7/§9 for the accepted-tech-debt XSS exposure this implies).
- **Company context**: a user authenticated across multiple companies selects an active company via `/api/v1/auth/select-company`; `active_company_id` is embedded in the resulting access token. Every backend query derives `company_id` scoping from this claim — never from a client-supplied query/body parameter.
- **Password handling**: bcrypt hashing (`passlib`); never returned in any response.

### 2.1 Auth Endpoints (`core/auth/router.py`)

| Method | Path | Description |
|---|---|---|
| POST | `/api/v1/auth/login` | Body: `{email, password}` → `{access_token, refresh_token, companies: [{id, name, role}]}`. Rate-limited to 10 requests/minute per IP (§20). |
| POST | `/api/v1/auth/select-company` | Body: `{company_id}` (with a valid access token) → a new `{access_token}` scoped to that company |
| POST | `/api/v1/auth/refresh` | Body: `{refresh_token}` → `{access_token}` — rejected (401) if the token's `gen` claim is older than the user's current generation counter (see logout below) |
| POST | `/api/v1/auth/logout` | Body: `{refresh_token}` → bumps the user's generation counter, revoking **every** refresh token ever issued to them ("logout everywhere"), not just this one. Backed by `core/auth/token_denylist.py`: a per-user counter in Redis when reachable, with an automatic in-process fallback if Redis is down — not a database table. The caller's still-valid access token keeps working until its own 15-minute expiry; only future `/refresh` calls are affected. |
| GET | `/api/v1/auth/me` | Returns current user profile + active company + role + permissions |

## 3. Authorization (RBAC)

- Every endpoint declares the permission(s) it requires (e.g., `crm:customers:write`), enforced via the `require_permission(...)` FastAPI dependency (`core/rbac/dependencies.py`), which reads the access token's `role`/`module_permissions` claims — the frontend never enforces this on its own (`PROJECT_AUDIT.md` confirms no frontend-only gating exists).
- Roles: `owner`, `manager`, `rep`, `viewer` per company. Permission strings follow an action-suffix convention (`core/rbac/permissions.py`): `:read` resolves to `viewer` tier and above, `:write`/`:approve`/`:settings:write` etc. resolve to `rep` tier and above (tightened per-permission where the code says so, e.g. Finance payments).
- Insufficient permission → `403 Forbidden` (§6). A handful of endpoints intentionally bypass `require_permission` for a documented reason — every one is called out explicitly in this document where it applies (the inbound webhook receivers in §18, the events-log endpoint in §9, the document-download endpoint in §8).
- Cross-company access attempts (token's company doesn't match the resource's `company_id`) → `404 Not Found` (not 403), to avoid confirming another company's resource exists.

## 4. General Conventions

- **Content type**: `application/json` for all requests/responses; file uploads use `multipart/form-data` against the dedicated upload endpoint (§8), which returns a `document_id` for use in other payloads.
- **Pagination**: cursor-based — `?limit=25&cursor=<opaque_cursor>` (`core/api/pagination.py`: an opaque base64 offset, decoded server-side). Response is `{items: [...], next_cursor: string | null}`. Applied consistently across every list endpoint in every module (Orders/Production/Installation/Finance were fixed in Version 2.30.0 — they previously accepted `limit`/`cursor` but hardcoded `next_cursor: null`, a gap this document itself flagged during the 2.29.0 sync pass and which was closed the same day).
- **Filtering/sorting**: list endpoints accept module-specific query filters (documented per endpoint below) and `?sort=field` / `?sort=-field` (descending) where sorting is supported.
- **Field naming**: `snake_case` in JSON payloads, matching Pydantic/Python naming.
- **Timestamps**: ISO 8601 UTC. Several modules store business dates (e.g. `scheduled_date`, `expense_date`, `valid_until`) as plain `String(10)` ISO date strings rather than a `Date` column — a pragmatic SQLite/Postgres portability choice, not an oversight; see `DATABASE_DESIGN.md` for which columns use which representation.
- **Money**: monetary fields are stored and returned as `Numeric(14,2)` decimal strings (e.g. `"1234.50"`) paired with a separate `currency` field (`"AZN"`), never a bare float — avoids floating-point precision loss.
- **IDs**: UUIDs as strings in JSON.
- **Idempotency**: no `Idempotency-Key` mechanism is implemented anywhere in the current codebase (this was a Phase-1 design intention, not built) — retried POSTs are not deduplicated. Worth noting as a real, currently-open gap rather than removing the concern outright.

## 5. Standard Response Envelope

Successful single-resource responses are the resource directly:

```json
{ "id": "...", "name": "...", "status": "..." }
```

List responses use the pagination envelope from §4:

```json
{ "items": [ { ... }, { ... } ], "next_cursor": "eyJvZmZzZXQiOjI1fQ==" }
```

## 6. Error Format (uniform across all modules)

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "quantity must be greater than 0",
    "details": [ { "field": "quantity", "issue": "must be greater than 0" } ],
    "request_id": "9f2e3a10-..."
  }
}
```

| HTTP Status | `error.code` | Meaning |
|---|---|---|
| 400 | `VALIDATION_ERROR` | Request body/query failed schema or business-rule validation |
| 401 | `UNAUTHENTICATED` | Missing/invalid/expired access token, or a revoked refresh token |
| 403 | `FORBIDDEN` | Authenticated but lacks permission for this action |
| 404 | `NOT_FOUND` | Resource doesn't exist, or belongs to a company the caller can't access |
| 409 | `CONFLICT` | State conflict (e.g. approving an already-approved order, a duplicate slab number) |
| 422 | Domain-specific (e.g. business-rule violation) | Syntactically valid payload rejected by the domain layer (e.g. an illegal status transition) |
| 429 | `RATE_LIMITED` | Rate limit exceeded (login only, today — see §20) |
| 500 | `INTERNAL_ERROR` | Unhandled server error; `request_id` is the correlation key for log lookup |

`request_id` is always present, set once at the FastAPI middleware layer in the core, not duplicated per module.

## 7. Company & User Management Endpoints (`core/companies/`, shared)

| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/api/v1/core/companies` | *(authenticated only, no specific permission)* | List the companies the caller has a role on — backs the company switcher |
| GET | `/api/v1/core/companies/users` | `core:companies:read` | List every user with a role on the active company, `{id, full_name, email, role}[]` — backs every "assign to a user" picker across the app (CRM Assigned Manager, Installation crew members, Task assignee) |

## 8. File Upload Endpoints (`core/storage/`, shared by every module)

| Method | Path | Permission | Description |
|---|---|---|---|
| POST | `/api/v1/core/documents` | `core:documents:write` (a single fixed permission — does **not** vary by `module`/`related_entity_type` in the body, despite that being the original Phase-1 intention) | Multipart upload (`module`, `related_entity_type`, `related_entity_id`, `file`) → stores the file (local disk in dev, Supabase Storage in production) and returns `{document_id, storage_path}` for attaching to any module's entity |
| GET | `/api/v1/core/documents/{id}` | *(authenticated only — any user in the same company can fetch the signed URL; there is no per-entity permission check, only a `document.company_id != current_user.active_company_id` ownership check → 404 on mismatch)* | Returns a signed, time-limited URL to the file |

**Upload constraints** (both added post-Phase-1, in a production-readiness pass): a 10 MB size cap (`MAX_UPLOAD_SIZE_BYTES`, → `400 VALIDATION_ERROR` if exceeded), and a content-type allowlist (`ALLOWED_UPLOAD_CONTENT_TYPES` — images, PDF, common office formats, plain text/CSV, and the audio/video types Communication Center attachments need), plus a narrow `application/octet-stream` carve-out gated on filename extension for DWG/DXF CAD files (browsers commonly report CAD files with no registered MIME type). Filenames are sanitized (`core/storage/client.py`'s `sanitize_filename()`) before ever being used to build a storage key, closing a path-traversal vector found and fixed in an earlier audit pass.

## 9. Event Log Endpoint (`core/events/`, introspection only)

| Method | Path | Access | Description |
|---|---|---|---|
| GET | `/api/v1/core/events?event_name=` | Owner role only — a hardcoded `if current_user.role != "owner": raise ForbiddenError(...)` check, **not** a registered `require_permission(...)` permission string | Read-only view into `event_log` for debugging "why did X happen"; hardcoded to the caller's active company and the most recent 100 rows (no `limit`/`cursor` pagination, and no `company_id` query param — company scoping is always implicit from the token, consistent with the rest of the API) |

Events themselves are never published via a public API endpoint — they are an internal integration mechanism between modules, not a client-facing feature.

---

## 10. CRM Module Endpoints (`/api/v1/crm/...`)

The real, shipped CRM is a **Customer / Lead / Task** model — not the generic Contact/Account/Deal/Pipeline-Stage design an earlier revision of this document sketched for Phase 1, which was never built that way. A `Customer` carries its own pipeline `status` directly (14 hardcoded stages — see `DATABASE_DESIGN.md` §4.1; there is no separate, per-company-configurable `pipeline_stages` table). `Contact` is a lightweight sub-entity of a Customer (e.g. a secondary contact person at a business customer), not an independent CRM object with its own account.

### Customers
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/crm/customers` | `crm:customers:read` | List, cursor-paginated. Query: `include_archived`, `assigned_manager_id`, `status`, `lead_source`, `search` (name/phone/email/social/company), `sort` (`name`/`created_at`/`updated_at`/`status`, `-` prefix = descending), `limit` (default 25, max 100), `cursor` |
| GET | `/crm/customers/export` | `crm:customers:read` | CSV download honoring the same filters as the list above (BOM-prefixed UTF-8 for correct Azerbaijani/Cyrillic rendering in Excel), capped at 10,000 rows |
| POST | `/crm/customers` | `crm:customers:write` | Create. Body: `name`, `type` (`individual`\|`business`, default `individual`), `assigned_manager_id?`, `lead_source?`, `advertising_campaign?`, `phone?`/`whatsapp?`/`instagram?`/`facebook?`/`email?`/`address?`/`company_name?`, `notes?`, `status?`, `tags?`, and an optional inline `contact: {full_name, email?, phone?}` |
| GET | `/crm/customers/{id}` | `crm:customers:read` | Retrieve |
| PATCH | `/crm/customers/{id}` | `crm:customers:write` | Partial update — same fields as create, all optional; validates `assigned_manager_id` resolves to a real member of the active company if provided |
| DELETE | `/crm/customers/{id}` | `crm:customers:write` | Archive (soft delete, sets `deleted_at`) — `409` if already archived. **No restore endpoint exists** (a known, deliberately-not-yet-built gap — see `PROJECT_AUDIT.md` B6) |
| GET | `/crm/customers/{id}/profile` | `crm:customers:read` | Aggregated profile: customer + contacts + attachments + activity timeline + linked Projects/Quotes/Orders/Payments |
| GET | `/crm/customers/{id}/notes` | `crm:notes:read` | List notes (activity-log entries of type `note`) |
| POST | `/crm/customers/{id}/notes` | `crm:notes:write` | Add a note. Body: `{body}` |
| GET | `/crm/customers/{id}/attachments` | `crm:attachments:read` | List attachments (files themselves uploaded via `POST /core/documents`, §8) |

### Leads
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/crm/leads` | `crm:leads:read` | List, cursor-paginated. Query: `status`, `source_channel`, `search` (name/phone/email/campaign), `sort` (`full_name`/`created_at`/`status`), `limit`/`cursor` |
| GET | `/crm/leads/export` | `crm:leads:read` | CSV download, same filters, capped at 10,000 rows |
| POST | `/crm/leads` | `crm:leads:write` | Create. Body: `full_name`, `source_channel` (one of `instagram`/`facebook`/`messenger`/`whatsapp`/`phone_call`/`website`/`office_visit`/`referral`/`other`), `email?`, `phone?`, `campaign?`, `assigned_manager_id?` |
| GET | `/crm/leads/{id}` | `crm:leads:read` | Retrieve |
| POST | `/crm/leads/{id}/convert` | `crm:leads:write` | Converts to a Customer (+ Contact) — `409` if already converted. Returns `{customer_id, contact_id}` |

### Tasks & Reminders
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/crm/tasks` | `crm:tasks:read` | List, cursor-paginated. Query: `assigned_to`, `status`, `priority`, `related_entity_type`/`related_entity_id`, `due_before`/`due_after`, `exclude_terminal`, `search` (title/description), `sort` (`title`/`due_date`/`priority`/`status`/`created_at`/`updated_at`), `limit`/`cursor` |
| POST | `/crm/tasks` | `crm:tasks:write` | Create. Body: `title`, `description?`, `priority?` (`low`/`medium`/`high`/`urgent`), `due_date?`/`remind_at?`, `assigned_to?`, `tags?`, `related_entity_type?`/`related_entity_id?` (polymorphic link, e.g. to a Customer), `is_recurring?`, `recurrence_rule?` (`daily`/`weekly`/`monthly`/`yearly`), `recurrence_interval?`, `recurrence_end_date?` |
| GET | `/crm/tasks/{id}` | `crm:tasks:read` | Retrieve |
| GET | `/crm/tasks/{id}/series` | `crm:tasks:read` | List every task generated from the same recurrence series |
| PATCH | `/crm/tasks/{id}` | `crm:tasks:write` | Update (`409`/`422` on an immutable or invalid recurrence change) |
| POST | `/crm/tasks/{id}/status` | `crm:tasks:write` | Transition status. Body: `{status, cancelled_reason?}`. Valid statuses: `pending`, `in_progress`, `done`, `cancelled` (`done`/`cancelled` terminal) |
| DELETE | `/crm/tasks/{id}` | `crm:tasks:write` | Delete (`204`) |
| GET | `/crm/task-notifications` | `crm:tasks:read` | List in-app notifications for the caller. Query: `unread_only` |
| POST | `/crm/task-notifications/check` | `crm:tasks:read` | Pull-triggered: generates any newly-due reminder/overdue notifications for the caller's own tasks (called on Dashboard/Tasks page load — this codebase has no background job scheduler, so due/overdue detection is check-on-read, not push) |
| POST | `/crm/task-notifications/{id}/read` | `crm:tasks:read` | Mark one notification read |

---

## 11. Stone Catalog Module Endpoints (`/api/v1/catalog/...`, Version 2.0+)

Built ahead of Sales per `ROADMAP.md`'s dependency chain. Permission suffix convention matches `core/rbac/permissions.py` (`:read` viewer tier, `:write` rep tier).

| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/catalog/brands?include_hidden=&search=&limit=&cursor=` | `catalog:brands:read` | List brands, cursor-paginated (`limit`/`cursor`/`next_cursor` added post-2.12; previously this endpoint had no pagination at all and silently capped at 50 rows — fixed in Version 2.26.0) |
| POST | `/catalog/brands` | `catalog:brands:write` | Create brand |
| GET | `/catalog/brands/{id}` | `catalog:brands:read` | Get brand |
| PATCH | `/catalog/brands/{id}` | `catalog:brands:write` | Update brand (incl. active/hidden status) |
| GET | `/catalog/collections?brand_id=&include_hidden=&search=` | `catalog:collections:read` | List collections (unbounded — no pagination; naturally small per-brand cardinality) |
| POST | `/catalog/collections` | `catalog:collections:write` | Create collection |
| GET\|PATCH | `/catalog/collections/{id}` | `catalog:collections:read\|write` | Get / update |
| GET | `/catalog/materials?brand_id=&collection_id=&status=&search=&sort=&limit=&cursor=` | `catalog:materials:read` | List materials, cursor-paginated (search across name/color/material_type/country) |
| POST | `/catalog/materials` | `catalog:materials:write` | Create material (validates brand/collection consistency) |
| GET\|PATCH | `/catalog/materials/{id}` | `catalog:materials:read\|write` | Get / update |
| GET\|POST | `/catalog/materials/{id}/images` | `catalog:materials:read\|write` | List/link images (`document_id` from a prior `POST /core/documents` upload) |
| GET\|POST | `/catalog/materials/{id}/documents` | `catalog:materials:read\|write` | List/link technical PDF / installation guide / cleaning guide |
| GET\|POST | `/catalog/materials/{id}/thicknesses` | `catalog:materials:read\|write` | List/add a thickness option for this Stone (Version 2.12) |
| DELETE | `/catalog/material-thicknesses/{id}` | `catalog:materials:write` | Remove a thickness option |
| GET\|POST | `/catalog/materials/{id}/sizes` | `catalog:materials:read\|write` | List/add a size option for this Stone (Version 2.12) |
| DELETE | `/catalog/material-sizes/{id}` | `catalog:materials:write` | Remove a size option |
| GET | `/catalog/materials/{id}/prices` | `catalog:price_lists:read` | Every price-list entry pricing this material, across every price list |
| GET | `/catalog/warehouses?include_hidden=` | `catalog:warehouses:read` | List warehouses (unbounded — no `LIMIT` clause at all today; genuinely returns every row, not just capped-but-unnoticed) |
| POST | `/catalog/warehouses` | `catalog:warehouses:write` | Create warehouse |
| GET\|PATCH | `/catalog/warehouses/{id}` | `catalog:warehouses:read\|write` | Get / update |
| GET | `/catalog/slabs?material_id=&warehouse_id=&status=&search=&sort=&limit=&cursor=` | `catalog:slabs:read` | List slabs, cursor-paginated (search across slab number/lot/barcode) |
| POST | `/catalog/slabs` | `catalog:slabs:write` | Create slab (computes `area_m2` server-side; rejects a duplicate `slab_number` within the company → `409`) |
| GET | `/catalog/slabs/{id}` | `catalog:slabs:read` | Get slab |
| PATCH | `/catalog/slabs/{id}/status` | `catalog:slabs:write` | Change status; illegal transitions (e.g. `sold` → `available`) → `409` |
| GET | `/catalog/price-lists?include_hidden=` | `catalog:price_lists:read` | List price lists (unbounded, same as warehouses) |
| POST | `/catalog/price-lists` | `catalog:price_lists:write` | Create price list |
| GET | `/catalog/price-lists/{id}` | `catalog:price_lists:read` | Get price list |
| GET\|PUT | `/catalog/price-lists/{id}/entries` | `catalog:price_lists:read\|write` | List entries / upsert one material's cost+sale price |

---

## 12. Sales Module Endpoints (`/api/v1/sales/...`)

The real Sales module is a deep nested resource tree, not a flat `quotes`/`sales_orders` pair. It has two parallel structures that sound similar but serve different purposes — **don't confuse them**:
- **Quotes** (a Project's pricing document): `Quote → QuoteSection → QuoteSectionItem` / `QuoteSectionMeasurement` — everything needed to price and PDF a customer-facing quote.
- **Project workspace** (the physical execution plan, independent of any specific Quote version): `Project → Room → ProjectItem → ProjectItemMeasurement` / `ProjectItemDrawing` / `ProjectItemPhoto` — tracks the actual pieces being fabricated, their real on-site measurements, drawings, and photos, across the life of the Project regardless of how many times the Quote gets re-priced.

An Order (§13) is created from an **accepted** Quote and deep-copies its Sections/Items/Measurements at that moment — editing an Order afterward never touches the source Quote.

### Projects
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/sales/projects` | `sales:projects:read` | List, cursor-paginated. Query: `customer_id`, `status`, `search`, `sort`, `limit`/`cursor` |
| POST | `/sales/projects` | `sales:projects:write` | Create. Body: `customer_id`, `name`, `project_type?` (`kitchen`/`bathroom`/`commercial`/`stairs`/`fireplace`/`other`), `address?`, `notes?`, `assigned_to?` |
| GET | `/sales/projects/{id}` | `sales:projects:read` | Retrieve |
| PATCH | `/sales/projects/{id}` | `sales:projects:write` | Update, incl. `status` (`active`/`completed`/`cancelled`) |

### Quotes
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/sales/projects/{project_id}/quotes` | `sales:quotes:read` | List quotes for a project (returns the full list; `next_cursor` always `null` — no real pagination need, quote count per project is small) |
| POST | `/sales/projects/{project_id}/quotes` | `sales:quotes:write` | Create. Body: `currency?`, `price_list_id?`, `valid_until?`, `internal_notes?`/`customer_notes?`, `vat_rate?` (default 18), `discount_type?` (`none`\|`percent`\|`fixed`), `discount_value?` |
| GET | `/sales/quotes/{id}` | `sales:quotes:read` | Retrieve with computed totals (`subtotal_gross`, `discount_amount`, `vat_amount`, `total_final`, plus internal-only `total_internal_cost`/`total_profit`/`profit_margin_pct`) |
| PATCH | `/sales/quotes/{id}` | `sales:quotes:write` | Update — rejected once the quote is `sent`/`negotiation`/`accepted` (immutable; a new version is required instead) |
| POST | `/sales/quotes/{id}/status` | `sales:quotes:write` | The **only** way to change quote status — there is no separate `/send` or `/accept` endpoint. Body: `{status}`. Valid transitions: `draft→{sent, rejected}`, `sent→{negotiation, accepted, rejected, expired}`, `negotiation→{sent, accepted, rejected, expired}`, `accepted→{rejected}` (cancellation only); `rejected`/`expired` are terminal. `409` on conflict (e.g. a referenced slab was reserved by another quote in the meantime) |
| GET | `/sales/quotes/{id}/pdf` | `sales:quotes:read` | Download the generated quote PDF (draft quotes are watermarked) |

### Quote Sections, Items & Measurements
| Method | Path | Permission | Description |
|---|---|---|---|
| GET\|POST | `/sales/quotes/{quote_id}/sections` | `sales:quotes:read\|write` | List/create sections (e.g. "Kitchen", "Island") |
| PATCH\|DELETE | `/sales/sections/{id}` | `sales:quotes:write` | Update/delete a section |
| GET\|POST | `/sales/sections/{section_id}/items` | `sales:quotes:read\|write` | List/create priced line items within a section — `item_type` is one of 21 curated values (countertop, island, wall_cladding, vanity, backsplash, edge_profile, sink_cutout, cooktop_cutout, faucet_hole, installation, transport, crane, tv_panel, bathroom_furniture, flooring, stairs, table, sink, fireplace, window_sill, material, other) |
| PATCH\|DELETE | `/sales/items/{id}` | `sales:quotes:write` | Update/delete an item |
| GET\|POST | `/sales/sections/{section_id}/measurements` | `sales:quotes:read\|write` | List/create measurement rows within a section (length/width/thickness/quantity/waste %, feeding the quote's pricing) |
| PATCH\|DELETE | `/sales/measurements/{id}` | `sales:quotes:write` | Update/delete a measurement |

### Project Workspace — Rooms & Project Items
| Method | Path | Permission | Description |
|---|---|---|---|
| GET\|POST | `/sales/projects/{project_id}/rooms` | `sales:projects:read\|write` | List/create Rooms (`room_type`: kitchen/bathroom/living_room/corridor/balcony/facade/yard/custom, plus legacy `staircase`/`exterior` kept valid for old data) |
| PATCH\|DELETE | `/sales/rooms/{id}` | `sales:projects:write` | Update/delete a Room — deleting cascades to its Project Items (and their Measurements/Drawings/Photos) |
| GET\|POST | `/sales/rooms/{room_id}/items` | `sales:projects:read\|write` | List/create Project Items within a Room — the curated 12-value physical-piece vocabulary (countertop, island, vanity, bathroom_furniture, tv_panel, table, wall_cladding, flooring, stairs, fireplace, window_sill, other), each with a Brand→Stone→Thickness→Size material reference |
| GET | `/sales/projects/{project_id}/items` | `sales:projects:read` | All Project Items across every Room in a project (backs the workspace's flat "Məmulatlar" tab) |
| PATCH\|DELETE | `/sales/project-items/{id}` | `sales:projects:write` | Update (incl. `production_status`/`installation_status`/`completion_status`: `pending`/`delivered`/`accepted`) or delete a Project Item |

### Project Workspace — Measurements, Drawings & Photos
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/sales/measurements?date_from=&date_to=` | `sales:projects:read` | **Company-wide** measurement list across all projects, date-range filterable — a distinct endpoint from the quote-section measurements above; backs the Dashboard's "measurements today" KPI |
| GET\|POST | `/sales/project-items/{item_id}/measurements` | `sales:projects:read\|write` | List/create measurement revisions for one Project Item — every `POST` is a new revision (`revision_number` increments), never an overwrite |
| PATCH\|DELETE | `/sales/project-item-measurements/{id}` | `sales:projects:write` | Edit a revision in place (e.g. attach `customer_signature_document_id` and mark `status: final`) or delete it |
| GET\|POST | `/sales/project-items/{item_id}/drawings` | `sales:projects:read\|write` | List/attach a DWG/DXF/sketch/PDF drawing (`document_id` from a prior upload) |
| DELETE | `/sales/project-item-drawings/{id}` | `sales:projects:write` | Detach a drawing |
| GET\|POST | `/sales/project-items/{item_id}/photos` | `sales:projects:read\|write` | List/attach a site photo |
| DELETE | `/sales/project-item-photos/{id}` | `sales:projects:write` | Detach a photo |

### Company Service Prices
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/sales/service-prices` | `sales:settings:read` | List the company's default sale/cost prices for non-material services (`edge_profile_per_lm`, `sink_cutout`, `cooktop_cutout`, `faucet_hole`, `installation_per_m2`, `transport`, `crane`) |
| PUT | `/sales/service-prices` | `sales:settings:write` | Upsert one service's price. Body: `{service_key, sale_price?, cost_price?}` |

---

## 13. Orders Module Endpoints (`/api/v1/orders/...`)

An Order is created from a single accepted Quote and owns its own independent, mutable copy of that Quote's Sections/Items/Measurements — the Quote is never touched again afterward. Depends on `crm`, `catalog`, `sales` per its manifest.

| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/orders?project_id=&customer_id=&status=&search=&sort=&limit=&cursor=` | `orders:read` | List, cursor-paginated |
| POST | `/orders` | `orders:write` | Create from an accepted quote. Body: `{quote_id}` — `400` if the source quote isn't `accepted` |
| GET | `/orders/{id}` | `orders:read` | Retrieve |
| PATCH | `/orders/{id}` | `orders:write` | Update `notes`/`production_notes`/`installation_notes`/`delivery_address`/`scheduled_production_date`/`scheduled_installation_date` — `409` if the order is in a terminal state |
| POST | `/orders/{id}/status` | `orders:write` | Transition status. Body: `{status, cancelled_reason?}`. Valid statuses and transitions: `waiting→{measuring, approved_for_production, cancelled}`, `measuring→{approved_for_production, cancelled}`, `approved_for_production→{in_production, cancelled}`, `in_production→{ready, cancelled}`, `ready→{delivered, installed, cancelled}`, `delivered→{installed, cancelled}`, `installed→{completed, cancelled}`; `completed`/`cancelled` terminal |
| GET | `/orders/{id}/sections` | `orders:read` | List sections (copied from the source quote at creation) |
| GET | `/orders/{id}/sections/{section_id}/items` | `orders:read` | List items within a section |
| GET | `/orders/{id}/sections/{section_id}/measurements` | `orders:read` | List measurements within a section |
| PATCH | `/orders/{id}/items/{item_id}` | `orders:write` | Update an order item's `production_status`/`installation_status`/`notes` — this is how Production/Installation completion cascades back onto the customer-facing Order view |

---

## 14. Production Module Endpoints (`/api/v1/production/...`)

One `WorkOrder` per Order (1:1, gated on the Order reaching `approved_for_production`), consuming every slab-linked `OrderItem` via a `WorkOrderItem` join row.

| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/production?status=&search=&limit=&cursor=` | `production:read` | List work orders, cursor-paginated |
| POST | `/production` | `production:write` | Create from an order. Body: `{order_id}`. `422` for `OrderNotReadyForProductionError`/`WorkOrderAlreadyExistsError`/`NoProductionItemsError`/`SlabNotReservedError` |
| GET | `/production/{id}` | `production:read` | Retrieve |
| GET | `/production/by-order/{order_id}` | `production:read` | Get the work order for a given order (`404` if none) |
| POST | `/production/{id}/status` | `production:write` | Transition status. Body: `{status, cancelled_reason?}`. Valid statuses/transitions: `queued→cutting→polishing→quality_check→completed`, with `cancelled` reachable from any non-terminal state; `completed`/`cancelled` terminal |
| GET | `/production/{id}/items` | `production:read` | List items, joined with the order item and slab details (`slab_number`, `description`, `quantity`, `unit`, `area_m2`) |

Completing a work order sells its consumed slabs (`catalog_slabs.status → sold`), marks each item's `production_status: done` on the parent Order, and advances the Order to `ready`; cancelling releases the slabs back to `available`.

---

## 15. Installation Module Endpoints (`/api/v1/installation/...`)

One `InstallationJob` per Order (1:1, gated on the Order reaching `ready`/`delivered`).

### Crews
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/installation/crews?status=` | `installation:read` | List crews (unbounded — no pagination) |
| POST | `/installation/crews` | `installation:write` | Create. Body: `{name, notes?}` |
| GET | `/installation/crews/{id}` | `installation:read` | Retrieve |
| PATCH | `/installation/crews/{id}` | `installation:write` | Update `name`/`status` (`active`/`inactive`)/`notes` |
| GET | `/installation/crews/{id}/members` | `installation:read` | List members, joined with user (`full_name`, `email`, `is_lead`) |
| POST | `/installation/crews/{id}/members` | `installation:write` | Add member. Body: `{user_id, is_lead?}` |
| DELETE | `/installation/crews/{id}/members/{member_id}` | `installation:write` | Remove member (`204`) |

### Installation Jobs
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/installation/jobs?status=&crew_id=&date_from=&date_to=&search=&limit=&cursor=` | `installation:read` | List, cursor-paginated |
| POST | `/installation/jobs` | `installation:write` | Create from an order. Body: `{order_id}`. `422` for `OrderNotReadyForInstallationError`/`JobAlreadyExistsError`/`CrewInactiveError` |
| GET | `/installation/jobs/{id}` | `installation:read` | Retrieve |
| GET | `/installation/jobs/by-order/{order_id}` | `installation:read` | Get the job for an order (`404` if none) |
| PATCH | `/installation/jobs/{id}` | `installation:write` | Update scheduling: `crew_id`, `scheduled_date`, `scheduled_time_slot`, `route_sequence`, `notes` |
| POST | `/installation/jobs/{id}/status` | `installation:write` | Transition status. Body: `{status, cancelled_reason?, completion_notes?}`. Valid statuses/transitions: `scheduled→en_route→in_progress→completed`, with `cancelled` reachable from `scheduled`/`en_route`/`in_progress`; `completed`/`cancelled` terminal |
| GET | `/installation/jobs/{id}/photos` | `installation:read` | List photos/signature for a job |
| POST | `/installation/jobs/{id}/photos` | `installation:write` | Attach a photo. Body: `{document_id, photo_type, caption?, sort_order?}` — `photo_type` one of `before`/`after`/`damage`/`signature`/`other` (a captured customer signature is stored as a `signature`-typed photo row, not a separate entity) |

### Notifications
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/installation/notifications?unread_only=` | `installation:read` | List in-app notifications for the caller (job assigned/rescheduled/status-changed — no email/SMS delivery exists for these) |
| POST | `/installation/notifications/{id}/read` | `installation:read` | Mark one read (uses the `read` permission tier, not `write`) |

Completing an installation job cascades `installation_status: done` onto every Order item and advances the Order to `installed`, mirroring Production's pattern.

---

## 16. Finance Module Endpoints (`/api/v1/finance/...`)

### Invoices
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/finance/invoices?customer_id=&status=&search=&limit=&cursor=` | `finance:invoices:read` | List, cursor-paginated |
| POST | `/finance/invoices` | `finance:invoices:write` | Create from an order. Body: `{order_id, due_date?, notes?}`. `422` for `OrderNotInvoiceableError`/`InvoiceAlreadyExistsError` — an order becomes invoiceable once it reaches `ready`/`delivered`/`installed`/`completed` |
| GET | `/finance/invoices/{id}` | `finance:invoices:read` | Retrieve. `balance_due` is not a stored column — it's computed on the fly as `total_amount - amount_paid` |
| GET | `/finance/invoices/by-order/{order_id}` | `finance:invoices:read` | Get the invoice for an order (`404` if none) |
| PATCH | `/finance/invoices/{id}` | `finance:invoices:write` | Update `due_date`/`notes` — `422` (`InvoiceImmutableError`) once the invoice is no longer `draft` |
| POST | `/finance/invoices/{id}/status` | `finance:invoices:write` | Transition status. Body: `{status, cancelled_reason?}` — the manually-settable statuses are only `sent`/`overdue`/`cancelled`; `partially_paid`/`paid` are set exclusively as a side effect of recording a payment (below), so `amount_paid` and `status` can never drift apart. Full status set: `draft`, `sent`, `partially_paid`, `paid`, `overdue`, `cancelled` (`paid`/`cancelled` terminal) |
| GET | `/finance/invoices/{id}/lines` | `finance:invoices:read` | List line items (a point-in-time snapshot copied from the order at invoice creation — no live FK back to order items) |
| GET | `/finance/invoices/{id}/payments` | `finance:invoices:read` | List payments recorded against this invoice |
| POST | `/finance/invoices/{id}/payments` | `finance:payments:write` (a distinct permission from `finance:invoices:write`) | Record a payment. Body: `{amount, method?, paid_at?, reference_note?}` — `method` one of `cash`/`bank_transfer`/`card`/`check`/`other` (default `cash`). `422` for `InvoiceImmutableError`/`OverpaymentError`/`InvalidPaymentAmountError` |

### Expenses
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/finance/expenses?order_id=&category=&date_from=&date_to=&limit=&cursor=` | `finance:expenses:read` | List, cursor-paginated |
| POST | `/finance/expenses` | `finance:expenses:write` | Create. Body: `{category?, amount, expense_date, order_id?, description?, currency?}` — `category` one of `materials`/`labor`/`transport`/`utilities`/`rent`/`other` (default `other`); `order_id` omitted means general company overhead, not tied to a job. `422` (`InvalidExpenseAmountError`) on a non-positive amount |
| GET | `/finance/expenses/{id}` | `finance:expenses:read` | Retrieve |

---

## 17. Reports Module Endpoints (`/api/v1/reports/...`)

Read-only cross-module analytics — this module owns no tables of its own; every endpoint aggregates other modules' data live. All six analytics endpoints share the same date-scoping query params: `period` (`7d`/`30d`/`90d`/`12m`/`custom`, default `30d`) or explicit `date_from`+`date_to` (which always wins over `period` when both are present). All require `reports:read`.

| Method | Path | Description |
|---|---|---|
| GET | `/reports/executive` | Cross-module executive KPIs (revenue, profit, margin, active customers, orders created, win rate) + revenue/profit trend + orders-by-status — backs the `/dashboard` executive snapshot (Version 2.24.0) |
| GET | `/reports/sales` | Sales pipeline analytics |
| GET | `/reports/production` | Production analytics (work-order cycle times, status breakdown) |
| GET | `/reports/installation` | Installation analytics (daily installs, delays, crew productivity) |
| GET | `/reports/finance` | Finance analytics (revenue/cost/profit/margin from real invoicing) |
| GET | `/reports/inventory` | **Added Version 2.25.0.** Live stock snapshot (total/available/reserved/in-production/sold slabs, available area, materials tracked/out-of-stock, active warehouse count, slabs by status, available slabs by warehouse) — **not** date-range filtered like the other five (current stock state has no meaningful history window), though it accepts the same query params for consistency |
| GET | `/reports/{report_type}/export/{export_format}` | One shared export endpoint covering **all six** report types above (`report_type` ∈ `executive`\|`sales`\|`production`\|`installation`\|`finance`\|`inventory`) in **both** `pdf` and `excel` formats — re-runs the matching analytics use case and streams back a file. `400` for an unknown `report_type`/`export_format` |

---

## 18. Communication Center Module Endpoints (`/api/v1/communication/...`, Version 2.7+ Real Integrations)

Unified omnichannel inbox (WhatsApp Business, Instagram Direct, Facebook Messenger, Email, SMS) integrated with CRM. Real provider connections (Meta Graph API for WhatsApp/Instagram/Messenger, SMTP+IMAP for email, Twilio for SMS, plus a generic webhook provider) were added in Version 2.9 on top of the exact same `ChannelProvider` interface (`send()`/`test_connection()`) established in Version 2.7 — a channel with no credential configured still uses `NullChannelProvider` (simulates a successful send), so every pre-2.9 behavior is unaffected by a channel never being upgraded to a real integration.

### Channels, Conversations & Messages
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/communication/channels?channel_type=&include_inactive=` | `communication:channels:read` | List channels (a company may have several of the same `channel_type`, e.g. multiple WhatsApp numbers) |
| POST | `/communication/channels` | `communication:channels:write` | Create a channel — publishes `ChannelCreated` |
| GET | `/communication/channels/{id}` | `communication:channels:read` | Get channel |
| PATCH | `/communication/channels/{id}` | `communication:channels:write` | Update display name/identifier/`is_active` (channels are deactivated, never deleted) |
| GET | `/communication/conversations?channel_id=&status=&assigned_to=&customer_id=&lead_id=&search=&sort=&limit=&cursor=` | `communication:conversations:read` | List conversations, cursor-paginated |
| POST | `/communication/conversations` | `communication:conversations:write` | Agent-initiated get-or-create for `{channel_id, external_contact_id}` — matches an existing Customer by the channel's mapped contact field, or **auto-creates a Lead** if unknown |
| GET | `/communication/conversations/{id}` | `communication:conversations:read` | Get conversation |
| PATCH | `/communication/conversations/{id}` | `communication:conversations:write` | Update status/assignment/tags, and link to a Customer/Lead/Project/Quote/Order |
| POST | `/communication/conversations/{id}/read` | `communication:conversations:write` | Resets the unread counter |
| GET | `/communication/conversations/{id}/messages` | `communication:conversations:read` | List messages, oldest first |
| POST | `/communication/conversations/{id}/messages` | `communication:conversations:write` | Send an outbound message through the channel's provider — `422` if the channel is deactivated |
| GET\|POST | `/communication/conversations/{id}/messages/{message_id}/attachments` | `communication:conversations:read\|write` | List/link attachments |
| GET\|POST | `/communication/conversations/{id}/notes` | `communication:notes:read\|write` | Internal, customer-invisible notes on a conversation |
| POST | `/communication/inbound` | `communication:conversations:write` | Receives one inbound message for `{channel_id, external_contact_id}` — get-or-creates the conversation, reopens it if closed, increments `unread_count` |
| GET | `/communication/templates?channel_type=&include_inactive=` | `communication:templates:read` | List message templates / quick replies (`channel_type=null` means usable on any channel) |
| POST | `/communication/templates` | `communication:templates:write` | Create |
| GET\|PATCH | `/communication/templates/{id}` | `communication:templates:read\|write` | Get / update |

### Credential Management (owner-only write, manager-tier read)
| Method | Path | Permission | Description |
|---|---|---|---|
| GET | `/communication/channels/{id}/credential` | `communication:channels:settings:read` | Get a channel's configured provider + **masked** config (secret fields reduced to their last 4 characters) + health status |
| PUT | `/communication/channels/{id}/credential` | `communication:channels:settings:write` | Configure/replace credentials — encrypted at rest (Fernet, keyed by `CHANNEL_CREDENTIALS_ENCRYPTION_KEY`); resets `health_status` to `unknown` |
| DELETE | `/communication/channels/{id}/credential` | `communication:channels:settings:write` | Remove the credential — the channel reverts to `NullChannelProvider` |

### Operational Endpoints
| Method | Path | Permission | Description |
|---|---|---|---|
| POST | `/communication/channels/{id}/test-connection` | `communication:integrations:write` | Calls the real provider's lightweight `test_connection()`; records `health_status`/`last_checked_at`/`last_error` |
| POST | `/communication/channels/{id}/imap-sync` | `communication:integrations:write` | Pull-triggered IMAP sync (email has no push/webhook) — fetches new messages since the last synced UID |
| GET | `/communication/queue?status=&limit=` | `communication:integrations:read` | Lists the outbound retry queue — a failed real-provider send never surfaces as a 500; the message is recorded `failed` and queued here |
| POST | `/communication/queue/process?limit=` | `communication:integrations:write` | Re-attempts every due queue entry (exponential backoff; permanently `failed` after `max_attempts`) — pull-triggered, same reason as CRM's task-notification check |
| GET | `/communication/integration-logs?channel_id=&provider=&direction=&limit=` | `communication:integrations:read` | Every provider interaction, `outbound` and `inbound` — backs Provider Diagnostics, Logs, and the Webhook Monitor at once |

### Public Webhook Endpoints (no `Authorization` header — trust comes from each provider's own signature scheme)
| Method | Path | Description |
|---|---|---|
| GET | `/communication/webhooks/meta/{channel_id}` | Meta's one-time subscription verification handshake |
| POST | `/communication/webhooks/meta/{channel_id}` | WhatsApp/Instagram/Messenger inbound messages and delivery/read callbacks — verifies `X-Hub-Signature-256` |
| POST | `/communication/webhooks/twilio/{channel_id}` | Twilio inbound SMS and delivery status callbacks — verifies `X-Twilio-Signature` |
| POST | `/communication/webhooks/generic/{channel_id}` | Generic partner webhook — verifies `X-Signature-256` |

A rejected signature returns `403` and is still logged (`direction=inbound`, `signature_valid=false`) rather than silently dropped. A processing error *after* signature verification succeeds never propagates as a 500 to the provider — it's caught, logged, and the endpoint still returns `200` (avoiding a provider-side retry storm).

---

## 19. AI Sales Assistant Module Endpoints (`/api/v1/ai/...`, Version 2.8+)

Provider-agnostic AI recommendations layered on CRM, Communication, Sales, and Tasks. **The AI module never performs a business action automatically** — every analysis call only creates `AIRecommendation` rows; a recommendation only ever affects Lead/Customer/Conversation/Task/Quote data if a human explicitly accepts it through the review endpoint, and even then that endpoint only ever updates the recommendation's own status. **No real LLM provider is integrated yet, by explicit design** — every provider name (`mock`, `openai`, `anthropic`, `gemini`, `ollama`, `azure_openai`) currently resolves to `MockAIProvider`, a deterministic heuristic engine.

| Method | Path | Permission | Description |
|---|---|---|---|
| POST | `/ai/leads/{lead_id}/analyze` | `ai:recommendations:write` | CRM Intelligence on a Lead — `lead_score`, `win_probability`, `priority_recommendation`, `next_best_action`, `follow_up_recommendation`, `lead_quality_explanation`, and conditionally `duplicate_lead`/`similar_customer`/`missing_info` |
| POST | `/ai/conversations/{id}/analyze` | `ai:recommendations:write` | Communication Intelligence on a Conversation's transcript — language/intent/sentiment/urgency, a summary, and conditionally entity extraction / link suggestions |
| POST | `/ai/quotes/{id}/analyze` | `ai:recommendations:write` | Sales Intelligence on a Quote against this company's own historical accepted quotes — discount guidance, delivery-complexity estimate, and conditionally product/cross-sell/upsell recommendations, margin-risk and price-anomaly detection |
| POST | `/ai/tasks/suggest` | `ai:recommendations:write` | Task Intelligence company-wide — task/reminder suggestions for stale leads and unanswered conversations, assignee suggestions, overdue-risk flags |
| GET | `/ai/recommendations?analysis_kind=&recommendation_type=&related_entity_type=&related_entity_id=&status=&provider=&sort=&limit=&cursor=` | `ai:recommendations:read` | List, cursor-paginated |
| GET | `/ai/recommendations/{id}` | `ai:recommendations:read` | Get one, including full audit fields (`provider`, `model`, `prompt`, `response`, `confidence_score`, `execution_time_ms`, `requested_by`) |
| POST | `/ai/recommendations/{id}/review` | `ai:recommendations:write` | Body `{decision: "accept"\|"reject"\|"edit", edited_response?}` — `422` if already reviewed (a decision is final) |
| GET | `/ai/dashboard` | `ai:dashboard:read` | Aggregated AI Dashboard: lead score distribution, pipeline health, at-risk customers, today's recommendations, usage statistics |

All endpoints publish a corresponding analysis-complete event (`LeadAnalyzed`, `ConversationAnalyzed`, `QuoteAnalyzed`, `TasksSuggested`) or review-decision event (`RecommendationAccepted`/`Rejected`/`Edited`), per the cross-cutting audit+event rule every module follows.

---

## 20. Rate Limiting

- `POST /api/v1/auth/login`: 10 requests/minute per IP (`core/rbac/rate_limit.py`, `FixedWindowRateLimiter`), confirmed accurate against the current code. In-process only today — a multi-instance deployment would need a shared store (Redis, already provisioned elsewhere in this stack) instead; documented as the known upgrade path in the code itself.
- No other endpoint in this API is rate-limited today. The Phase-1 design intention of a generous default rate limit across all authenticated endpoints, and per-company limits on AI-job submission, were never implemented — noted here as an open item, not a false claim of current behavior.

## 21. Mobile Parity Notes

- No endpoint in this specification has a web-only equivalent that bypasses it — every feature in the Next.js frontend is backed by one of the documented endpoints above, callable identically by a future mobile client with the same bearer-token mechanism. This structural property is real (verified during `PROJECT_AUDIT.md`'s backend review: no server-rendered-only or session-cookie-only path exists), even though no mobile client has been built yet to actually exercise it.
- **Correction from the original Phase-1 draft of this document**: no endpoint anywhere in the current codebase uses the `202 Accepted` + `{job_id, status_url}` polling pattern originally proposed for long-running operations (AI analysis, report export). Both are synchronous today — AI analysis because `MockAIProvider` is fast deterministic heuristics, not a real model call; report export because it's a direct database aggregation, not a queued job. Should a real LLM provider or a genuinely slow export ever change that, the async pattern remains the documented target design, but it is not current behavior.
