# G-STONE ERP — Product Roadmap

_Date: 2026-06-30_
_Status: CRM Version 1.0 is frozen as of commit `bec2def`. No new business features are being added until this roadmap is approved._

This is a product-perspective review of the application as it stands, and a proposal for what comes next, split into three horizons: **1.1** (polish the frozen CRM), **1.2** (deepen the CRM into a daily operating tool), and **2.0** (the first new ERP module beyond CRM, per the modular architecture frozen in `PROJECT_ANALYSIS.md`).

---

## What Version 1.0 actually is

A multi-company (G-STONE GALLERY, KORONA PREMIUM, NEOLITH BAKU), trilingual (Azerbaijani default, Russian, English fallback) CRM with:
- Auth, RBAC (owner/manager/rep/viewer), full audit log, internal event bus
- A stone-industry Customer model (contact channels, company affiliation, 14-stage sales pipeline, free-text notes) and Lead capture across 9 channels
- A Dashboard with real pipeline counters
- Search, sort, inline quick-actions, keyboard shortcuts, and a global quick-create menu tuned for 8-hour daily use
- A documented, fixed set of known gaps (`RELEASE_CHECKLIST.md`) — Critical/High items are fixed; Medium/Low items are intentionally deferred, and several of them are exactly what 1.1 picks up

This document does not repeat that audit; it turns the deferred items (and obvious next product steps) into a sequenced plan.

---

## Version 1.1 — Finish what 1.0 started

**Theme:** close the gaps already identified during the Release Candidate and Business Optimization passes. No new entities, no new modules — these are completions and hardening of existing CRM features.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Assigned Manager picker** — a real dropdown of company users on the customer form/profile, replacing the current free-typed UUID (which has no UI at all today) ✅ **Delivered 2026-07-13** | High | Direct: managers can't currently be assigned through the UI despite the backend fully supporting and validating it — this is the single most visible gap between "built" and "usable" | Needs a new `GET` company-users list endpoint (small, additive) | S |
| **Save/action confirmation feedback** (toasts) for status changes, note saves, customer creation — creation ✅ delivered in 2.8.1; status-change/note-save on the Customer detail page ✅ **delivered 2026-07-13** | High | Removes the "did that actually save?" doubt during fast, repeated daily use — directly named in `UI_UX_GUIDELINES.md` and never implemented | None | S |
| **"Load more" pagination UI** wired to the cursor API that already exists server-side ✅ **Delivered 2026-07-14** | Medium | Prevents silent data loss once a company passes ~25–100 customers; the backend contract is already correct, only the UI never consumes it | None — backend done in `0f6eba6` | S |
| **Upload content-type allowlist** (images/PDF/office docs) | Medium | Security hardening flagged as Medium in `RELEASE_CHECKLIST.md` (M1); low urgency since uploads are never served as executable content today, but cheap to close before the upload feature sees real traffic | None | S |
| **Shareable/persisted list filters** (status, search, sort reflected in the URL) ✅ **Delivered 2026-07-14** | Medium | A manager re-opening "my In Production customers" shouldn't have to re-filter every time; also makes filtered views bookmarkable/shareable between teammates | None | S |
| **Bulk actions on the Customer list** (multi-select archive, multi-select status change) | Medium | Saves real time when cleaning up stale inquiries in bulk, a recurring weekly task for a gallery manager | None | M |
| **Remove orphaned `customer.type` exposure** and reconcile the legacy `Contact` sub-entity with the stone-industry profile fields (M2/M8 in the checklist) | Low | Pure tech-debt cleanup; no user-facing value, but reduces confusion for future development | None | S |
| **Refresh-token revocation** (logout-everywhere, Redis-backed denylist) | Low | Security hardening (M7); low urgency for a 3-company internal tool, but Redis is already provisioned and unused | Redis wiring (infra already exists, just unused) | S |

**Suggested sequencing within 1.1:** Manager picker and toasts first (highest visible value, smallest effort), then pagination UI and filter persistence, then bulk actions, then the two cleanup/hardening items.

---

## Version 1.2 — Make the CRM the place reps actually live

**Theme:** the gap between "a CRM that stores records" and "a CRM that runs a sales rep's day." These are still CRM-only features (no new top-level module), but each is a meaningfully sized addition, not a polish item.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Tasks & Reminders** (due-date follow-ups, assigned to a rep, surfaced on the Dashboard and as overdue alerts) ✅ **Delivered 2026-07-06** | High | The single biggest missing daily-use loop: today nothing reminds a rep to call a customer back. This is the most-requested kind of feature in any CRM rollout | Manager picker (1.1, for assignment) | M |
| **Per-company configurable pipeline stages & lead sources** (today the 14 statuses and 9 channels are hardcoded platform-wide, even though the architecture and DB design always intended per-company configuration) | High | KORONA PREMIUM and NEOLITH BAKU may not want G-STONE GALLERY's exact stone-fabrication pipeline; this was a stated architectural goal not yet delivered | A minimal company-settings admin screen (doesn't exist yet) | M |
| **Role-scoped Dashboard & list views** ("my customers" vs. "all customers" depending on role) | Medium | Reps shouldn't have to filter out the whole company's pipeline to see their own work | Manager picker (1.1) | M |
| **Export Customers/Leads to CSV** ✅ **Delivered 2026-07-10** | Medium | Standard expectation for any CRM; used for offline reporting, accounting handoff, marketing list pulls | None | S |
| **Email/notification delivery for Task reminders** (in-app today only; email or SMS next) | Medium | Extends Tasks & Reminders beyond "must be logged in to see it" | Tasks & Reminders (this version); an email provider integration | L |
| **Attachment previews/thumbnails** (images shown inline, not just a filename) | Low | Quality-of-life for the Attachments card, especially for site/product photos | None | M |
| **Mobile-responsive verification pass** (the frozen architecture is API-first/mobile-ready by design; this is validating that promise, not building a mobile app) | Low | De-risks an eventual native mobile client without committing to building one yet | None | S |

**Suggested sequencing within 1.2:** Tasks & Reminders first (it's the headline feature), then configurable pipeline/lead-source settings (it unblocks the other two companies from being properly served), then role-scoped views, then the smaller items.

---

## Version 2.0 — The first modules beyond CRM

**Theme:** per the frozen plugin architecture (`PROJECT_ANALYSIS.md` §9), CRM was always module one of ten. Version 2.0 is where the platform stops being "a CRM" and starts being "an ERP." **Approved architectural change:** a new **Stone Catalog** module is inserted before Sales — quotations cannot be real without real stone brands, collections, slabs, pricing, and technical specifications to quote *from*, so Sales now depends on the Catalog rather than going straight from CRM to Quotes.

**Revised dependency chain (approved):**

```
CRM
 ↓
Tasks & Reminders        (Version 1.2)
 ↓
Stone Catalog             (Version 2.0 — new, moved ahead of Sales)
 ↓
Sales (Quotes)
 ↓
Orders
 ↓
Production
 ↓
Installation
 ↓
Finance
 ↓
Reports
```

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Stone Catalog module** — brands, collections, slabs/lots, technical specifications (dimensions, finish, material type), and price lists ✅ **Delivered 2026-06-30** | High | The foundational data Sales cannot function without: today there is no system of record for what G-STONE GALLERY, KORONA PREMIUM, and NEOLITH BAKU actually sell or at what price — every quote is currently built from memory or an external spreadsheet | CRM + Tasks & Reminders (1.2) | XL |
| **Sales module: Quotes** (line items drawn from the Catalog, pricing, linked to a Customer) ✅ **Delivered 2026-07-01** | High | The direct next step after a customer reaches "Quote Sent" in the pipeline today — right now that status exists with nothing behind it, and a quote without real catalog items isn't a real quote | Stone Catalog | L |
| **Orders** (created from an accepted Quote, triggers the `OrderApproved` event already named in the architecture) ✅ **Delivered 2026-07-01** | High | Closes the loop from "won deal" to an actual order record; `OrderApproved` is already a documented event other future modules (Production) will subscribe to | Sales (Quotes) | L |
| **Production module: work orders** (fabrication/cutting jobs linked to an Order, consuming specific slabs/lots from the Catalog) ✅ **Delivered 2026-07-04** | High | Core stone-fabrication workflow; the "In Production" pipeline status already exists in CRM with nothing operational behind it yet — and now has real slab data to consume against, via the Catalog | Orders (+ reads from Stone Catalog for slab/lot consumption) | XL |
| **Installation module: scheduling & crew assignment** ✅ **Delivered 2026-07-04** | Medium | Matches the "Installation Scheduled" / "Installed" statuses already live in the CRM pipeline today with no operational system behind them | Production | L |
| **Finance module: invoicing & payments** ✅ **Delivered 2026-07-06** | High | Closes the revenue loop; "Payment Received" already exists as a CRM status with nothing behind it; invoice line items now trace back to real Catalog pricing | Orders + Installation completion events | L |
| **Reports module: cross-module dashboards** ✅ **Delivered 2026-07-04 (partial — see change log)** | Medium | Only becomes valuable once Catalog/Sales/Production have real data flowing | All of the above | M |

**Why Stone Catalog before Sales, specifically:** a quote is only as real as what's being quoted. Without brands, collections, slabs, technical specs, and pricing as actual data, "Sales" would mean typing arbitrary line-item text into a Quote — no different from a paper quote. The Catalog is now the dependency that makes every downstream module (Sales, Production, Finance) reference real, priced, trackable stone instead of free text. This also absorbs what earlier roadmap drafts called "Inventory" (slab/lot tracking) — in a stone-gallery business, the catalog *is* the inventory; modeling them as one module avoids duplicating slab/lot data across two places.

**Purchasing** (suppliers and purchase orders, used to restock the Catalog) remains on the longer-term module list but is not on this critical path — it depends on the Stone Catalog existing first and can be sequenced after Version 2.0 lands, once restocking becomes the active bottleneck rather than initial cataloging.

**Out of scope for this document:** Marketing and AI modules remain on the original ten-module list but are deliberately not sequenced into 1.1/1.2/2.0 — Marketing depends on Sales/CRM data maturity, and AI is explicitly designed to be the *last* module added (per `PROJECT_ANALYSIS.md` §9), once there's real document/image volume from Sales, the Stone Catalog, and Installation to act on.

---

## Version 2.7 — Communication Center

**Theme:** a new module beyond the original ten-module list (`PROJECT_ANALYSIS.md` §9's CRM/Sales/Inventory/Purchasing/Production/Installation/Finance/Reports/Marketing/AI) — a unified omnichannel inbox, requested directly rather than derived from the earlier roadmap sequencing.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Communication Center** — unified inbox for WhatsApp Business (multiple numbers), Instagram Direct, Facebook Messenger, Email, and SMS, integrated with CRM ✅ **Delivered 2026-07-06** | High | Every one of these channels today is a separate app an agent has to check by hand, with no link back to the customer/lead/order it's about. A unified inbox closes that gap and gives every conversation a CRM identity | CRM (customer identification, auto-Lead creation), Sales + Orders (conversation linking) | XL |

**Explicit non-goal for this version:** no real WhatsApp Business/Meta Graph/SMTP/SMS-gateway API integration. Every channel type resolves to a `NullChannelProvider` that simulates a successful send — see `modules/communication/infrastructure/providers/` for the abstraction every real integration will later implement without any change to a use case, another module, or the core. `POST /communication/inbound` stands in for where a real, signature-verified webhook will eventually land.

---

## Version 2.8 — AI Sales Assistant

**Theme:** the last module on the original ten-module list (`PROJECT_ANALYSIS.md` §9 named AI as deliberately sequenced last), now built once there was real Lead/Customer/Conversation/Quote/Task volume across CRM, Communication, Sales, and Tasks for it to act on.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **AI Sales Assistant** — provider-agnostic recommendation engine covering CRM Intelligence (lead scoring, win probability, priority, next best action, duplicate/similar detection, missing-info detection), Communication Intelligence (language/intent/sentiment/urgency detection, summarization, entity extraction, link suggestions), Sales Intelligence (product/cross-sell/upsell recommendations, discount guidance, margin-risk and price-anomaly detection, delivery complexity), Task Intelligence (task/reminder/assignee suggestions, overdue-risk detection), and a dedicated AI Dashboard ✅ **Delivered 2026-07-06** | High | Every one of these signals today requires a rep to notice it manually — a stale lead, an urgent unanswered message, a quote priced 30% off historical norms. Surfacing them as reviewable recommendations closes that gap without ever taking an action a human didn't approve | CRM, Stone Catalog, Sales, Orders, Communication | XL |

**Explicit non-goal for this version:** no real LLM provider integration. Every provider name (`mock`, `openai`, `anthropic`, `gemini`, `ollama`, `azure_openai`) resolves to `MockAIProvider`, a deterministic heuristic engine — see `modules/ai/infrastructure/providers/` for the abstraction every real provider will later implement without any change to a use case, another module, the schemas, or the frontend. **AI never performs a business action automatically**: every recommendation requires an explicit Accept/Reject/Edit, and accepting one only ever changes the recommendation's own status — never a Lead, Customer, Conversation, Task, or Quote row — which is enforced structurally by `ReviewRecommendationUseCase`, not just promised by the UI.

---

## Version 2.8.1 — UX & Platform Polish

**Theme:** a frontend-only cross-cutting pass over everything built since Phase 1, rather than a new module — closing the gap between the individually-shipped screens (each internally consistent) and a single, cohesive design system across all nine modules now that the full module chain (CRM through AI) exists to polish.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Design system foundation**: Montserrat (replacing a never-actually-loaded "Inter" reference), every color token converted from hardcoded hex to CSS custom properties, dark mode (light/dark toggle in the header, persisted, no-flash-on-load), expanded spacing/shadow tokens ✅ **Delivered 2026-07-06** | High | Every existing `bg-bg`/`text-text-primary`/etc. class across the whole app now resolves through a variable instead of a fixed hex value — dark mode and any future re-theming is a token swap, not a per-page rewrite, exactly as `UI_UX_GUIDELINES.md` section 3 anticipated | None (frontend-only) | L |
| **Table improvements**: column resize, column visibility toggle, sticky headers, and saved filter presets (all `localStorage`-backed, no new backend endpoint) — full toolkit on the four highest-traffic list pages (Leads, Customers, Invoices, Tasks); sticky-header + horizontal-scroll-instead-of-clip fix mechanically applied to the other ~14 list-view tables platform-wide ✅ **Delivered 2026-07-06** | High | Column resize/visibility and saved filters were the most-requested table gaps; sticky headers were already promised by `UI_UX_GUIDELINES.md` section 5.4 but only 2 of 24 tables actually had them | Design system foundation | L |
| **Form UX**: `Button` gained a `loading` prop (spinner + `aria-busy`, backward compatible), `Field` components gained `aria-invalid`/`aria-describedby` and a red error border, a new global `Toast` primitive for success/error confirmations (wired into the Leads capture/convert flows as the demonstrated pattern) ✅ **Delivered 2026-07-06** | Medium | Removes the need for every page to hand-roll its own loading/validation styling — one shared primitive improvement cascades to every existing caller | Design system foundation | M |
| **Mobile responsiveness**: the sidebar (previously always-rendered at a fixed 224px, unusable below `lg`) becomes a slide-over drawer with a hamburger toggle below the `lg` breakpoint, closable via Escape/backdrop-click/explicit close button, per `UI_UX_GUIDELINES.md` section 7 ✅ **Delivered 2026-07-06** | High | The single highest-value mobile fix: every page depends on `AppShell`, so this one change makes navigation usable app-wide on a phone/tablet, not just one screen at a time | None | M |
| **Accessibility baseline**: a global `:focus-visible` ring (replacing scattered per-component focus classes), a skip-to-content link, `aria-current`/`aria-sort`/`aria-expanded` on nav/sort/menu controls ✅ **Delivered 2026-07-06** | Medium | Per `UI_UX_GUIDELINES.md` section 8 ("focus states must be visible... all interactive elements reachable via keyboard") — the existing dropdowns (Company/Language switcher, Quick Create) already had Escape/outside-click handling from earlier sessions; this pass makes that baseline consistent everywhere else | Design system foundation | M |
| **Print/PDF layout**: global `@media print` rules (navigation/buttons hidden, palette forced to light regardless of the active on-screen theme, table page-break hints), plus a dedicated Print button on the Invoice detail page ✅ **Delivered 2026-07-06** | Medium | The Invoice detail page is the natural print target in an ERP; previously printing any page just dumped the full app chrome (sidebar, buttons, dark background if dark mode was active) onto paper | Design system foundation | S |
| **Dashboard performance**: derived arrays (task/customer/lead filtering and sorting) on the main Dashboard wrapped in `useMemo`, so unrelated re-renders no longer re-run the same O(n) filters/sorts every time ✅ **Delivered 2026-07-06** | Low | A safe, behavior-preserving optimization — same data, same output, less redundant computation | None | S |

**Explicit non-goals for this version:** no business logic changed anywhere (verified by the full 427/427 backend suite passing untouched, since zero backend files were modified); no existing API contracts changed; the full resize/visibility/saved-filter table toolkit was applied to 4 flagship list pages rather than mechanically forced onto all ~24 tables in the app (many of which are small, non-paginated, in-page item lists inside detail views — e.g. invoice line items, work-order line items — where that toolkit doesn't add value) — the reusable toolkit itself (`components/ui/data-table.tsx`) is available for any future list page to adopt.

---

## Version 2.9 — Real Integrations

**Theme:** the Communication Center (2.7) shipped with every channel type wired to `NullChannelProvider` "by explicit design" — a deliberate placeholder pending real credentials. Version 2.9 is that follow-through: real WhatsApp/Instagram/Messenger (Meta Graph API), SMTP/IMAP email, and Twilio SMS providers, plus a generic webhook provider for arbitrary partner systems, an encrypted per-channel credential store, and the operational surface (health checks, a retry queue, provider diagnostics, a webhook monitor) a production integration actually needs.

| Feature | Priority | Business value | Dependencies | Complexity |
|---|---|---|---|---|
| **Real provider implementations**: WhatsApp Business Cloud API, Instagram Messaging API, Messenger Send API (all three via Meta Graph API), SMTP (send) + IMAP (inbound sync), Twilio SMS, and a generic outbound/inbound webhook provider — each a new `ChannelProvider` implementation, the interface itself completely unchanged ✅ **Delivered 2026-07-06** | High | Closes the gap Communication Center's own docstrings named explicitly: "once a real provider exists, its webhook handler would call ReceiveInboundMessageUseCase directly... under its own signature-verification" | Communication Center (2.7) | XL |
| **Per-company provider configuration**: encrypted credential storage (Fernet, one `ChannelCredential` row per channel — multiple accounts per provider already falls out of Communication Center's existing "several channels of the same type" design), masked read-back (secrets never round-trip to the frontend), connection testing, health monitoring ✅ **Delivered 2026-07-06** | High | A real integration is only as trustworthy as its secret handling — this is the part that turns "we called an HTTP API" into something safe to actually deploy | Real provider implementations | L |
| **Real messaging operations**: signature-verified inbound webhooks (Meta HMAC-SHA256, Twilio HMAC-SHA1, generic HMAC-SHA256), delivery/read status callbacks reconciled onto the original `Message` row, a retry queue for failed sends (exponential backoff, permanent failure after `max_attempts`), and a Provider Diagnostics / Logs / Webhook Monitor admin surface (one `IntegrationLogEntry` table, discriminated by direction/action) ✅ **Delivered 2026-07-06** | High | "Real inbound/outbound messaging" only matters operationally if failures are visible and recoverable, not silent | Real provider implementations | L |
| **CRM auto-sync preserved unchanged**: inbound messages from every real provider (webhook or IMAP sync) flow through the exact same `GetOrCreateConversationUseCase`/`ReceiveInboundMessageUseCase` that already auto-identify a known Customer or auto-create a Lead for an unknown sender — no new sync logic needed, since that pattern was already provider-agnostic ✅ **Delivered 2026-07-06** | Medium | Confirms Communication Center's "reuse CRM's own CreateLeadUseCase" design decision was actually provider-agnostic, not just Null-provider-agnostic | Communication Center (2.7) | S |

**Explicit non-goals for this version:** no real Meta/Twilio/SMTP account was available in this development environment to test against, so every provider is verified with a mocked HTTP/SMTP/IMAP layer (real request-building, real signature verification, real response parsing — just not a live third-party network call) rather than an actual WhatsApp/Twilio account; wiring in real production credentials is an operator configuring `PUT /communication/channels/{id}/credential`, not a code change. `ChannelProvider.send()`'s signature is unchanged; `test_connection()` was added as a new concrete (non-abstract) method with a trivial default, which every existing provider (`NullChannelProvider`) inherits for free — additive, not a breaking change to the interface. Media attachments for outbound sends are sent by URL reference (the file already uploaded via the existing core Documents pipeline) rather than raw binary passed through `ChannelProvider.send()`, since extending that fixed method signature was explicitly out of scope.

---

## Version 2.9.1 — Enterprise Polish

**Theme:** a platform-wide production-readiness audit across all ten shipped modules (CRM, Tasks, Communication, AI, Sales, Orders, Production, Installation, Finance, Reports) — no new features, no business-logic changes, no API contract changes. Scope: duplicated code, query efficiency, error handling, validation, logging, security, and frontend UI/UX consistency, accessibility, and responsive layout.

| Fix | Area | Business value |
|---|---|---|
| Application-level logging added to Communication's webhook/integration/send/queue/IMAP-sync error paths (`logger.warning`/`logger.exception` alongside the existing `IntegrationLogEntry` DB record) | Backend — Communication | Previously every failure in these paths was visible only inside the app's own DB tables; a misconfigured provider or a signature-verification storm now also shows up in stdout/log aggregation, where an on-call engineer would actually look first |
| Sales quote acceptance's slab availability check/reserve/release batched into a single query (`Slab.id.in_(...)`) instead of one `db.get()` per line item, and the three near-identical loops consolidated behind one `_quoted_slabs_by_id` helper | Backend — Sales | Removes an N+1 query pattern that scaled with quote size; behavior (including the "slab not found" error message) is unchanged, verified by the existing `tests/sales/test_quotes.py` suite passing unmodified |
| Fixed a real bug: `handleCancel`/`handleAdvance`/`handleSend`/`handleMarkOverdue`/`handleComplete`/`handleSaveDetails` and related write-actions on the Orders, Production, Finance Invoice, and Installation Job detail pages had no error handling at all — a failed API call (validation error, network error) failed completely silently, with no toast, no message, nothing | Frontend — Orders/Production/Finance/Installation | The highest-value fix in this release: a user clicking "Cancel" or "Advance" on a stuck record previously had no way to know why nothing happened. All now surface the real error via the existing `useToast` primitive |
| Repeated inline `overflow-x-auto rounded-lg border border-border bg-surface` / sticky-`thead` class strings across 8 list pages (Catalog Brands/Materials/Price Lists/Slabs/Warehouses, Orders, Production, Sales Projects) replaced with the shared `tableScrollShellClass`/`stickyTheadClass` constants already used by Customers/Leads/Tasks/Invoices | Frontend — Catalog/Orders/Production/Sales | One definition instead of eight copies; brings these list pages in line with the shared-constant pattern Version 2.8.1 already established elsewhere |
| Toast dismiss button's hardcoded `aria-label="Dismiss"` moved to a translation key (`common.dismiss`), the one gap in an otherwise fully-translated app | Frontend — shared `Toast` component | Closes the last untranslated user-facing string found in the audit |
| Completed two changes an interrupted prior session had left half-done: `crm/tasks/new`'s success toast was wired up (`useToast` imported, called) but never actually invoked, and its submit button still used the old manual disabled/text-swap pattern instead of `Button`'s `loading` prop; the `customerCreated`/`taskCreated` translation keys the two pages' `t()` calls already referenced didn't exist yet in any of the three locale files | Frontend — CRM | Without this fix, `az.json`/`ru.json`/`en.json` would have silently rendered a missing-key fallback (or thrown, depending on `next-intl` config) the first time either toast fired in production |
| Hardened the production-boot guard (`_guard_against_insecure_defaults` in `core/bootstrap/app_factory.py`) to also refuse to start with the default `CHANNEL_CREDENTIALS_ENCRYPTION_KEY` outside development, alongside the existing JWT-secret check | Backend — Core | Version 2.9's per-channel credential encryption is only as safe as its key; this closes the same class of "public default in source" risk the JWT guard already covered |
| Unhandled-exception handler in `core/api/errors.py` now logs the exception with the same `request_id` returned to the caller | Backend — Core | Lets support correlate a user-reported request ID with the actual server-side stack trace |
| `ConfigureChannelCredentialUseCase` now validates a provider's required config fields at configuration time (clean `400`) instead of only failing the first time something tries to use an incomplete credential; `SendMessageUseCase`/`TestChannelConnectionUseCase` catch provider-construction failures gracefully rather than raising an unhandled `500` | Backend — Communication | Fail-fast, better-scoped errors for a real operational mistake (a half-filled-in credential form) |

**Audit findings considered and explicitly not acted on:** ~86 free-text schema fields across 15 files with no `max_length` (flagged as a defense-in-depth nicety, not an active vulnerability — no injection surface exists since no raw SQL string formatting was found anywhere in the codebase); Reports' Python-side aggregation (`sum()`/grouping over fetched ORM rows instead of SQL-side `SUM`/`GROUP BY`) — a real future scaling concern but a rewrite risky enough, for analytics correctness, to defer rather than rush under a "no business logic changes" mandate; and a platform-wide breadcrumb-vs-"← Back to X" link inconsistency on detail pages (`UI_UX_GUIDELINES.md` §6.1 technically wants breadcrumbs everywhere, only Customers has them) — a real but larger-scoped deviation, flagged here as a known follow-up rather than patched piecemeal across ten pages without i18n keys for each breadcrumb path.

**Verification:** full backend suite (489/489 passing, including a new test covering the hardened credential-key boot guard), frontend `tsc --noEmit` clean, frontend production build clean (all 41 routes), and a live end-to-end smoke test against the real dev database and both running servers — login, company switch, every touched list/detail page loaded with zero console errors, and a real customer created through the UI confirmed the new toast/i18n wiring fires correctly end-to-end.

---

## Version 2.9.2 — Production Readiness & G-STONE Onboarding

**Theme:** Phase 3 — preparing the platform for real daily use by G-STONE GALLERY. A full application-perspective audit (navigation, branding, every CRUD flow, search/filter/sort/pagination/export, permissions, company switching, responsive/dark-mode/i18n behavior, loading/empty/error states, dev artifacts) rather than a new module or feature set. No new entities, no business-logic changes.

| Fix | Area | Business value |
|---|---|---|
| Sales Projects' "Create Project" customer picker requested `limit=200` against an endpoint capped at 100, so it 400'd on every load and rendered empty — Create was unusable from that form | Frontend — Sales | Release-blocking: this was the single form standing between a rep and creating a new project, found only by driving the real flow end-to-end rather than reading the code |
| Orders list `sort` query parameter existed in the API and frontend wrapper but was silently ignored by `OrderRepository.list()` (always sorted by `created_at desc`) | Backend — Orders | A dead API contract is worse than no contract — callers had no way to know sorting silently did nothing; now wired to a real field map, matching `ProjectRepository`'s existing pattern |
| Sales Quotes had no UI to edit VAT rate, discount, currency, validity date, or notes after creation, despite the backend endpoint and translation keys already existing from an earlier, unfinished pass | Frontend — Sales | Closes a real CRUD "Edit" gap on the core quoting workflow — every quote was previously stuck with creation-time defaults forever |
| Frontend `Quote.discount_type` TypeScript type didn't match the backend (`"percentage"` vs. the backend's `"percent"`) | Frontend — Sales | A latent bug that would have silently zeroed every percentage discount the moment a real UI (this pass's own Quote Settings panel) started sending it |
| Catalog Brands and Warehouses had no archive/restore UI despite the backend fully supporting it (Materials already used the same status toggle) | Frontend — Catalog | Closes a real CRUD "Archive/Restore" gap for two catalog entities |
| Missing favicon; every route showed the identical static browser-tab title | Frontend — Branding/Navigation | Directly user-visible polish for a tool used 8 hours a day — a monogram favicon added, and the tab title now reflects the active section on in-app navigation (a hard-reload/direct-URL limitation remains, documented as a follow-up rather than fixed, since the `(app)` route group is fully client-rendered) |
| Sales Projects list lacked the status filter and sortable columns every sibling list page already had, despite the backend already supporting both | Frontend — Sales | Consistency fix, mechanical once confirmed backend-ready |
| Two dead, unused translation keys (`sales.serviceSettings`/`sales.servicePriceUpdated`) removed from all three locale files; freed slots repurposed for the new Quote Settings UI strings | Frontend — i18n | Dev-artifact cleanup found during the audit |

**Audit findings considered and explicitly not acted on** (each a deliberate scope call, not an oversight — see `PRODUCTION_READINESS_REPORT.md` for full reasoning): no icon library in the sidebar despite `UI_UX_GUIDELINES.md` calling for one (a real design-system decision); no tablet-width icon-only collapsed sidebar; no client-side pagination UI on Customers/Leads/Orders/Production/Invoices/Slabs despite cursor-based backend support (the same item already named and deferred in Version 1.1, reconfirmed still open, not a new regression); no frontend permission-gating of write buttons by role (the backend's `require_permission` is the real, authoritative control — this is a UX gap, not a security one); backend validation-error messages aren't localized when they bypass the frontend's translation fallback; Production and Finance Invoices list pages have no backend `sort` parameter at all (unlike Orders/Sales Projects, which just needed wiring); Sales Quote Sections/Measurements support delete-and-recreate only, not in-place edit.

**Verification:** full backend suite (492/492 passing), frontend `tsc --noEmit` clean, frontend production build clean (all 41 routes), and a live end-to-end Playwright smoke test against the real dev database and both running servers — login, company selection, every touched page loaded with zero console/HTTP errors, a real brand created and archived/restored through the new toggle, and a real project → quote created with its new Quote Settings panel edited and confirmed persisted across a page reload.

---

## Version 2.9.3 — Production Readiness follow-up

**Theme:** a second pass closing out the remaining Phase 3 findings from four independent research audits (i18n consistency, responsive/dark-mode, navigation/breadcrumbs/icons, CRUD completeness) that hadn't yet been acted on when 2.9.2 shipped. No new entities, no business-logic changes.

| Fix | Area | Business value |
|---|---|---|
| `az.json`'s `catalog.tableDefault` key was left as literal English `"Default"` despite Azerbaijani being the app's default locale | Frontend — i18n | The last untranslated user-facing string found in this audit round, seen by every AZ user on the Price Lists table |
| Dashboard/Reports chart gridlines and data-point strokes hardcoded to light-mode hex (`#E2E5EA`/`#FFFFFF`) instead of the app's `--color-border`/`--color-surface` CSS custom properties | Frontend — charts | Chart gridlines were nearly invisible against a dark background; every other themed element already used these same variables, this was the one component that didn't |
| Only the Customers detail page had real breadcrumbs; the other nine detail pages (Orders, Production, Catalog Materials, Catalog Price Lists, CRM Tasks, Finance Invoices, Installation Jobs, Sales Projects, Sales Quote builder) used a plain "← Back to X" link — a gap named as far back as the 2.9.1 changelog entry | Frontend — navigation | Closes a real, long-flagged UX consistency gap across the whole app rather than one page at a time; a shared `Breadcrumb` component now backs all ten pages, with the Quote builder showing the full three-level trail |
| Eight now-orphaned `backTo*`/`back` translation keys removed from all three locale files after the breadcrumb replacement above | Frontend — i18n | Dev-artifact cleanup, key parity maintained across `en.json`/`az.json`/`ru.json` |

**A finding from an earlier research pass was investigated and declined, not fixed:** CRM Customer archive was reported as missing a "Restore" button in the UI, on the assumption that the existing `PATCH /crm/customers/{id}` endpoint could already un-archive a customer. On inspection, `UpdateCustomerUseCase` never touches `deleted_at` — only `ArchiveCustomerUseCase` does, and no restore use case exists anywhere in the backend. Building one would be new business logic, out of scope for a production-readiness pass; recorded here as a genuine, not-yet-built gap rather than silently wired up against a capability that doesn't exist.

**Also considered and explicitly not acted on** (same reasoning as 2.9.2's deferred list): a sidebar icon library, despite `UI_UX_GUIDELINES.md` calling for one — a real design-system decision (icon set choice, sizing, ~20 nav entries and every page header touched), not a bug fix.

**Verification:** full backend suite (492/492 passing, unchanged since no backend files were touched), frontend `tsc --noEmit` clean, frontend production build clean (all 41 routes), and a live end-to-end Playwright smoke test against the real dev database and both running servers — login, company selection, every touched list page, and five of the ten breadcrumb-refactored detail pages exercised with real records (Customers, Sales Projects, CRM Tasks, Catalog Price Lists, Sales Quote builder), zero console errors, and the dark-mode chart fix confirmed via computed style against the live toggle.

---

## Version 2.10.0 — G-STONE Sprint 2 (Simplified Navigation & Layihə-Centric Workflow)

**Theme:** a usability-driven restructuring for G-STONE GALLERY's office staff, not a new module. The prior ~20-item flat sidebar read as an accounting/ERP tool rather than a gallery's day-to-day workflow tool; this sprint re-anchors the UI around "Layihə" (Project) as the object office staff actually think in terms of, and pushes 1C's territory (warehouse/stock/accounting) out of the daily-use surface without deleting any of it.

| Change | Area | Business value |
|---|---|---|
| Primary sidebar cut from ~20 flat entries to 9 module-level sections (İdarə Paneli, Müştərilər, Layihələr, Materiallar, İstehsal, Quraşdırma, Mesajlar, Hesabatlar, Ayarlar), per `UI_UX_GUIDELINES.md` §6.1's "module-level primary / sub-nav secondary" navigation model, which the sidebar had never actually followed until now | Frontend — navigation | New office staff can hold the whole app in their head in one glance instead of scanning 20 similarly-styled links |
| A new `SectionTabs` component provides secondary in-page tabs for everything that used to have its own sidebar entry (Müştərilər/Leads/Tapşırıqlar under CRM; Materiallar/Brendlər under Catalog; Layihələr/Sifarişlər under Sales) | Frontend — navigation | Every page that existed before is still one click away, just regrouped under the section it logically belongs to |
| A new `/settings` hub page groups everything that's genuinely back-office and out of the daily workflow — Channels, Templates, Integrations (messaging admin), Warehouses, Slabs, Price Lists (Catalog admin), and Orders/Invoices/Expenses/AI Assistant (oversight convenience links) — using translation keys (`settings.*Desc`) that had been added in a prior session but never wired to any page | Frontend — navigation | 1C's territory (warehouse/stock/accounting) is visually deprioritized without removing a single feature; nothing was deleted, only regrouped, so no data or capability was lost |
| Seven new Sales `item_type` values — countertop (Mətbəx dəzgahı), island (Ada), tv_panel (TV paneli), bathroom_furniture (Hamam mebeli), flooring (Döşəmə), stairs (Pilləkən), table (Masa) — added to `VALID_ITEM_TYPES`/`MATERIAL_ITEM_TYPES`/`ITEM_TYPE_DEFAULT_UNIT` in `modules/sales/domain/value_objects.py`, alongside the pre-existing wall_cladding/vanity/backsplash types | Backend — Sales | Office staff can now record a project as the physical pieces a customer actually ordered (a countertop, an island, a TV panel), each with its own material, production status, and installation status via the existing per-item fields (`OrderItem`/`QuoteSectionItem` already carried `material_id`, `production_status`, `installation_status` — this sprint only extended the controlled vocabulary, no schema change) |
| Every item-type value across the Quote builder and Order detail pages now renders a real translated label (`sales.itemType_*`) instead of the raw English slug (e.g. `wall_cladding`) that was displayed verbatim before | Frontend — i18n | Fixes a real, previously-shipped i18n gap in the same pass that added the new types |
| Material creation form: `thickness`/`dimensions` changed from free-text inputs to curated dropdowns (`SUGGESTED_THICKNESSES_MM`, `SUGGESTED_SIZES_MM`) with a manual "Other" override, presenting entry as a Brand → Stone name → Thickness → Size cascade | Frontend — Catalog | Reduces free-text drift ("12mm" vs "12 mm" vs "12") ahead of a future official supplier-catalog import; deliberately does not hardcode real manufacturer spec sheets, only common defaults |
| `nav.catalog` label changed from "Daş Kataloqu"/"Каталог камня"/"Stone Catalog" to "Materiallar"/"Материалы"/"Materials" across all three locale files | Frontend — i18n | Plainer, less "catalog software" wording for office staff |

**Deliberately not done in this pass** (recorded here rather than silently skipped): no backend schema migration was added to move `OrderMeasurement`/`QuoteSectionMeasurement` (thickness/length/width) to item-level ownership — they remain section-level rows alongside their section's items, which already satisfies the daily workflow (one section per physical piece is the natural usage pattern) without a migration. No Catalog reference tables for standardized Brand→Stone→Thickness→Size were added; the cascade is presented in the UI over the existing free-text-capable `StoneMaterial` columns, ready to be backed by real supplier data later without another UI rewrite. No content beyond navigation labels, item types, and the Material form was reworded — a full copy pass across the ~500 remaining `az.json` keys was out of scope for this sprint.

**Verification:** full backend suite (492/492 passing), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes including the new `/settings` route).

---

## Version 2.11.0 — Measurement & Room Management

**Theme:** makes "Layihə" (Project) genuinely the primary business object, per Sprint 2's intent, by giving it real internal structure: a Project now contains Rooms ("Məkanlar"), each Room contains Project Items ("Məmulatlar" — the physical pieces being fabricated), and each Project Item owns its own Material selection, Measurement history, Drawings, and Photos. Built entirely inside the existing Sales module — **no new module was created** — since Rooms/Items are a project-planning structure that belongs alongside Project/Quote, not a separate concern. An initial plan to build Measurement and Room as standalone modules was corrected mid-sprint specifically to avoid that duplication.

| Change | Area | Business value |
|---|---|---|
| New `Room` entity (`sales_rooms`) scoped to a Project — kitchen/bathroom/living_room/staircase/exterior/custom, with an optional custom label | Backend — Sales | Office staff record a project as the physical spaces the customer's job actually covers, not an abstract list |
| New `ProjectItem` entity (`sales_project_items`) scoped to a Room — the curated piece vocabulary from Sprint 2 (countertop, island, sink *(new)*, tv_panel, vanity, wall_cladding, flooring, stairs, table, other) plus a Brand→Stone→Thickness→Size Material selection (an existing `catalog_materials` row, never free text) | Backend — Sales | Each physical piece being fabricated is now a first-class record with its own material, quantity, production status, and installation status — matching how a stone gallery actually plans a job |
| New `ProjectItemMeasurement` entity with **revisions**: every recorded measurement is a new `revision_number`, never an overwrite, so a re-measure never loses the original site visit's numbers. Records length/width/thickness, quantity, computed area, measurer name, measurement date, notes, and an attachable customer signature (`customer_signature_document_id`) | Backend — Sales | A full audit trail of every site visit for a piece, with proof the customer signed off on the final numbers before fabrication |
| New `ProjectItemDrawing` (DWG/DXF/sketch/PDF) and `ProjectItemPhoto` entities, both backed by the existing core `documents` polymorphic store — same pattern as `catalog_material_documents`/`catalog_material_images` | Backend — Sales | Technical drawings and site photos live with the piece they belong to, reusing the one shared file-storage pipeline instead of a parallel one |
| `core/storage/router.py`'s upload content-type allowlist extended to accept DWG/DXF (by MIME type, or by filename extension when the browser reports the common `application/octet-stream` fallback) | Backend — Core | DWG/DXF uploads (required for Drawings) were previously rejected by every module's shared upload endpoint |
| Project detail page rebuilt into a tabbed workspace: Overview, Rooms, Measurements, Drawings, Photos, Production, Installation, Completion | Frontend — Sales | Replaces the old single quotes-list page; Rooms is the primary nested CRUD surface (Room → Project Item → Material/Measurements/Drawings/Photos), the other tabs are project-wide rollups and a computed completion summary. Primary app navigation (the 9-section sidebar from Sprint 2) is unchanged — this is entirely within the existing "Layihələr" page |

**Verification:** full backend suite (514/514 passing — 492 prior + 22 new), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes).

---

## Version 2.12.0 — Production-Ready Material Selector

**Theme:** turns Sprint 2/3's Brand → Stone → Thickness → Size *idea* into a real, normalized data model. Previously "thickness" and "dimensions" were two free-text columns on the Material row itself, seeded from a generic suggestion list — one Stone could only ever have one thickness and one size. Now a Stone can offer several of each, and every Project Item records exactly which one it was built with. No new module; entirely inside Catalog (the two new option tables) and Sales (the two new `ProjectItem` FK columns).

| Change | Area | Business value |
|---|---|---|
| New `MaterialThickness`/`MaterialSize` tables, each a normalized set of options belonging to one Stone (`catalog_materials`), with full CRUD (add/list/delete) mirroring the existing `MaterialImage`/`MaterialDocument` sub-resource pattern | Backend — Catalog | A single Stone (e.g. Calacatta Gold) can now offer 12mm and 20mm, in 3200x1600mm and 1600x800mm, without duplicating the whole Material row per combination |
| `ProjectItem` gained `material_thickness_id`/`material_size_id` (nullable FKs to the new tables) | Backend — Sales | Every physical piece records exactly which Thickness/Size option it was built with, not just which Stone |
| Material creation form no longer collects thickness/dimensions at Stone-creation time; the Material detail page gained "Thickness Options"/"Size Options" cards (list + add + delete) where those options are managed afterward | Frontend — Catalog | Structured, repeatable option management instead of a one-shot free-text pair baked into the Stone itself; the legacy `thickness_mm`/`dimensions` columns are untouched for backward compatibility with existing Materials |
| Project workspace's "Add Item" form: Brand → **searchable** Stone (debounced server-side search via the existing `search` query param) → Thickness → Size, the latter two populated from the selected Stone's own options | Frontend — Sales | Matches the exact brief: thickness/size choices are scoped to what that specific Stone actually offers, and finding a Stone among hundreds no longer means scrolling a giant dropdown |
| `SUPPORTED_BRANDS` curated suggestion list (NEOLITH, MARAZZI THE TOP, SAPIENSTONE, INALCO, ANATOLIA, BELENCO, COANTE) added as `<datalist>` suggestions on the Brand creation form | Frontend — Catalog | Recognizes the named supplier brands without hardcoding any manufacturer technical specs or forcing every company to use exactly this list — `Brand.name` stays free text |
| Sprint 2's `SUGGESTED_THICKNESSES_MM`/`SUGGESTED_SIZES_MM` (previously feeding the now-removed Material-creation dropdowns) repurposed as `<datalist>` suggestions on the new per-Stone Thickness/Size add-forms | Frontend — Catalog | No dead code left behind from the form's redesign |

**Verification:** full backend suite (522/522 passing — 514 prior + 8 new), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes).

---

## Version 2.13.0 — The Complete Project Workflow

**Theme:** turns the Project workspace into the full operational workflow G-STONE GALLERY actually runs a job through, end to end. Rooms ("Məkanlar") and Project Items ("Məmulatlar") each get their own dedicated tab (previously only reachable nested inside the Rooms tab); a new Materials rollup tab answers "what Stones does this job actually use"; and "Təhvil" (handover/customer acceptance) becomes a real per-piece status, not just a project-level stat. Entirely inside the existing Sales module — no new module, no new nav entries (this is all within the "Layihələr" workspace from Sprint 2).

| Change | Area | Business value |
|---|---|---|
| Project workspace tabs reordered/expanded to 10: Ümumi, Məkanlar, **Məmulatlar** (new), **Materiallar** (new), Ölçülər, Çertyojlar, Fotolar, İstehsal, Quraşdırma, **Təhvil** (renamed from "Completion") | Frontend — Sales | Matches the exact daily workflow office staff actually follow through a job, not just a random tab order |
| New "Məmulatlar" tab: a flat, project-wide table of every piece across every Room (room, type, stone, quantity, notes) | Frontend — Sales | Answers "what are we making for this whole job" in one screen, without opening every Room card individually |
| New "Materiallar" tab: pieces grouped by the exact Stone + Thickness + Size combination used, with item count and total quantity per group | Frontend — Sales | Answers "which Stones does this job need, and how much of each" — the question that drives ordering slabs from a supplier |
| `ProjectItem.completion_status` (new nullable column, `pending`/`delivered`/`accepted`) | Backend — Sales | "Təhvil" (handover to the customer) is now tracked per physical piece, distinct from `production_status` (is it fabricated) and `installation_status` (is it fitted) — a piece can be installed but not yet formally accepted by the customer |
| "Təhvil" tab rebuilt from four static stat cards into a per-item editable table (same pattern as the existing Production/Installation tabs), plus a small summary row | Frontend — Sales | Matches requirement #8: completion status belongs to each Məmulat, not just a project-wide rollup |
| Two new Project Item types — `fireplace` (Kamin) and `window_sill` (Pəncərə altlığı) — and two dropped from the curated picker but kept valid for backward compatibility (`sink`, plus `bathroom_furniture`/`vanity` both now offered together) | Backend + Frontend — Sales | Matches Sprint 5's authoritative 12-type "Məmulat" list exactly |
| Four new Room types — `corridor` (Dəhliz), `balcony` (Eyvan), `facade` (Fasad), `yard` (Həyət) — splitting the old catch-all `exterior`; `staircase`/`exterior` kept valid (not offered in the picker) since staircase work is now modeled as an `ITEM_TYPE_STAIRS` piece within any Room | Backend + Frontend — Sales | Matches Sprint 5's authoritative 8-type "Məkan" list; no Room saved before this sprint stops working |
| 6 new backend tests (new item/room types accepted, legacy types still accepted, completion_status create/update/default) | Backend — Sales | Confirms the vocabulary expansion is additive, not breaking |

**Deliberately not done in this pass:** no database migration was needed for the new item/room type vocabulary — `item_type`/`room_type` were never DB-level enums, only Pydantic-validated strings, so widening `VALID_ITEM_TYPES`/`VALID_ROOM_TYPES` is a pure code change. Only `completion_status` needed a migration (one nullable column, no FK).

**Verification:** full backend suite (528/528 passing — 522 prior + 6 new), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes).

---

## Version 2.15.0 — Sprint 6: Operational Dashboard Redesign

**Theme:** the Dashboard had been a CRM stats screen (customer/lead counters, leads-by-channel) since Phase 1, unchanged even after Sales/Orders/Production/Installation/Finance/Tasks all shipped real daily-operations data of their own. This sprint replaces it with an operations-first view answering "what should our team do today?" instead of "how many records exist" — frontend-led, with one small Sales endpoint added to back it. No new module.

| Change | Area | Business value |
|---|---|---|
| Time-of-day greeting ("Sabahınız xeyir"/afternoon/evening) using the logged-in user's real first name, replacing the generic "Welcome back, {name}" + role line | Frontend — Dashboard | A small but real signal that the app knows who's using it right now, not just that someone is |
| Four daily-ops KPI cards: measurements today, work orders in production, installations scheduled tomorrow, overdue work (overdue tasks + overdue orders) | Frontend — Dashboard | Replaces "how many customers exist" with "what does today actually look like" — the real question office staff open the Dashboard to answer |
| `GET /api/v1/sales/measurements` — company-scoped, date-range-filterable list of measurements | Backend — Sales | The "measurements today" KPI needed a company-wide query; the only existing measurement endpoint was scoped to one Project Item |
| Five dashboard sections: Today's Tasks, Upcoming Installations, Overdue Projects, Notifications (merged task + installation), Recent Inquiries | Frontend — Dashboard | Turns the Dashboard into an actual daily worklist — click straight into the task, job, or order that needs attention — instead of a set of static counters with no next action |
| Old customer/lead counters (active/archived customers, open/converted leads, leads-by-channel) removed from the Dashboard | Frontend — Dashboard | Superseded by the daily-ops framing; the underlying data and full detail is still one click away on the Customers/Leads pages, unaffected by this change |
| 1 new backend test (company-wide measurement list, date-range filtering) | Backend — Sales | Confirms the new endpoint's date filtering is correct |

**Verification:** full backend suite passing, frontend `tsc --noEmit` clean, frontend production build clean.

---

## Version 2.16.0 — CSV Export for Customers & Leads

**Theme:** closes the last remaining Version 1.2 line item — CSV export was named a "standard expectation for any CRM" back when 1.2 was scoped, and had been passed over every session since 2026-07-01 in favor of the Version 2.0+ module chain. Small, self-contained, no dependencies, matching its original S-complexity estimate.

| Change | Area | Business value |
|---|---|---|
| `GET /api/v1/crm/customers/export` and `GET /api/v1/crm/leads/export` — CSV downloads honoring the same status/lead-source/channel/search/sort filters as their list endpoints | Backend — CRM | Offline reporting, accounting handoff, and marketing list pulls no longer require manually copying rows out of the browser |
| "Export CSV" button on the Customers and Leads list pages, exporting exactly the currently-filtered/sorted view | Frontend — CRM | The export always matches what the user is looking at, not a separate unfiltered dump |
| UTF-8 output with a leading BOM | Backend — CRM | Excel (the realistic destination for this file) renders Azerbaijani/Cyrillic characters correctly instead of guessing a legacy codepage |
| Reuses the existing `crm:customers:read`/`crm:leads:read` permissions and the existing `apiDownload` blob-download helper (the same mechanism Reports' PDF/Excel export and Sales' Quote PDF already use) | Backend + Frontend — CRM | No new permission, no new download mechanism — one more consumer of patterns already proven elsewhere in the codebase |
| 6 new backend tests (CSV shape, filter-respecting content, unauthenticated access) | Backend — CRM | Confirms the export mirrors list-endpoint filtering exactly, not just "returns some CSV" |

**Verification:** full backend suite passing, frontend `tsc --noEmit` clean, frontend production build clean.

---

## Version 2.17.0 — Assigned Manager Picker

**Theme:** closes the highest-priority remaining Version 1.1 item — the customer form/profile showed a raw manager UUID (or nothing at all) instead of a real picker, despite the backend already fully validating `assigned_manager_id` against real company members. Small, self-contained, no new module.

| Change | Area | Business value |
|---|---|---|
| "Assigned Manager" dropdown (`GET /api/v1/core/companies/users`, already existing from Installation's crew-assignment work) added to the Customer creation form and made editable on the Customer profile page, replacing the raw `customer.assigned_manager_id ?? "Unassigned"` UUID string that was previously rendered verbatim to office staff | Frontend — CRM | Removes the single most visible gap between "built" and "usable" named back in Version 1.1: managers could not be assigned through the UI at all, even though the backend has validated and audited this field since Phase 2 |
| Backend fix: `UpdateCustomerUseCase` previously treated `assigned_manager_id: null` in a PATCH body identically to the field being omitted (both read as Python `None`), so there was no way to ever un-assign a manager through the API. A `clear_assigned_manager` flag, set from `"assigned_manager_id" in payload.model_fields_set`, now distinguishes "field not sent" from "field explicitly cleared" — the fix is scoped to this one field only, the same PATCH-omission semantics for every other Customer field are untouched | Backend — CRM | The manager picker's "Unassigned" option would otherwise have silently no-opped — found and fixed via a live API smoke test against the running dev server, not just by reading the code |
| Toast confirmations wired into the Customer detail page for status change, manager change, note save, and the profile-notes save — the one Customer-detail gap left over from 2.8.1's toast rollout (creation already had a toast; the detail page's four write actions did not) | Frontend — CRM | Closes the other half of the 1.1 "toasts for status changes, note saves" line item — removes the "did that actually save?" doubt on the page office staff spend the most time editing |
| 2 new backend tests (explicit-null unassign succeeds; omitting the field in an unrelated PATCH leaves the existing assignment untouched) | Backend — CRM | Regression coverage for the PATCH-semantics fix, using the same `app_client`/`owner_headers` pattern as the existing assigned-manager validation tests |

**Verification:** full backend suite (537/537 passing — 535 prior + 2 new), frontend `tsc --noEmit` clean, frontend production build clean, and a live smoke test against the real dev database and running backend (login, company selection, create a customer with a manager assigned, unassign via explicit null, reassign, and confirm an unrelated field update leaves the assignment untouched) — the PATCH-semantics bug was only found this way, not by the test suite or by reading the code.

---

## Version 2.18.0 — Full-App UX Audit

**Theme:** a from-scratch audit of every visible screen from a real G-STONE office employee's perspective — six parallel research passes (Dashboard/CRM/nav, Catalog/Settings, the Sales Project workspace, Orders/Production/Installation, Finance/Reports, Communication/AI), each reading every page and locale key in its scope rather than sampling. No new module, no new entities, no business-logic changes — frontend-only except one narrowly-scoped backend fix the audit's own live testing surfaced.

| Fix | Area | Business value |
|---|---|---|
| **Every date in the app rendered in English regardless of the active locale** — `lib/format.ts`'s `formatDate`/`formatDateTime` were hardcoded to `toLocaleDateString("en-US", ...)`, and the Installation Calendar's month header had its own separate `"en-US"` hardcode. Both now read the same `g_erp_locale` `localStorage` key the `LocaleProvider` already persists, via a new `activeDateLocale()` helper | Frontend — global | The single highest-impact finding of the audit: every date on every page ("Yaradıldı: Jul 13, 2026") was silently ignoring the user's language choice. Now renders "13 iyl 2026" in Azerbaijani, verified against Node's ICU output before shipping |
| **Reports charts showed raw English "No data for this period."** regardless of locale — `CategoryBarChart`/`TrendChart` (`components/ui/charts.tsx`) had no `emptyLabel` prop (unlike the sibling `StatusBarList`, which already had one); six call sites across Executive/Sales/Finance/Installation Reports now pass the translated `noDataPeriod` string. `compactNumber()`'s chart-tooltip number formatting had the same `"en-US"` hardcode, fixed the same way | Frontend — Reports, charts | Every chart on every Reports page would show this for any date range with no data — a very likely occurrence for KORONA PREMIUM/NEOLITH BAKU or any custom range |
| **Illegible dark-mode section headers** — four detail pages (CRM Task, Finance Invoice, Order, Sales Quote builder) used `bg-text-primary` (which is near-white in dark mode) with `text-white`, rendering near-invisible white-on-white headers. Swapped to `bg-primary`, which has consistent contrast in both themes | Frontend — Tasks, Finance, Orders, Sales | A real visual bug, not just a wording issue — these section headers ("Line Items", "Series", etc.) were unreadable for anyone using dark mode |
| **Identical button text on "open cancel dialog" vs. "confirm cancel"** — Orders, Production, Installation, and Finance Invoice detail pages all had a toggle button and a confirm button sharing the exact same label (e.g. "Sifarişi ləğv et" appearing twice on screen at once), making the first click look like it did nothing. The toggle button now hides itself once the cancel-reason card is open, in all four places | Frontend — Orders, Production, Installation, Finance | Removes a real point of confusion on the most consequential action on each of these detail pages |
| **Silent failures on write actions across the Sales Project workspace, the Quote builder, Installation's Kanban board, and Installation's Crews page** — `handleCreateRoom`, `handleDeleteRoom`, `handleCreateItem`, `handleDeleteItem`, `handleItemStatusChange`, all seven measurement/drawing/photo callbacks, the Quote builder's `handleAddItem`/`handleUpdateItemPrice`/`handleDeleteItem`/`handleAddMeasurement`/`handleDeleteMeasurement`/`handleStatusChange`, Kanban's `handleAdvance`, and Crews' `handleCreateCrew`/`handleToggleStatus`/`handleAddMember`/`handleRemoveMember` had no `try/catch` at all — a failed request (validation error, network blip) did nothing visible. All now surface the error via the existing `useToast`/`setError` pattern each file already used for its read-path failures | Frontend — Sales, Installation | The exact silent-failure bug class Version 2.9.1 fixed on Orders/Production/Finance/Installation detail pages, but the Sales Project workspace (built afterward, in Version 2.11.0–2.13.0) and Installation's Kanban/Crews pages had never been covered by that pass |
| **Raw UUIDs shown to users instead of names/numbers** — Orders list's "Layihə" column, Order detail's "from quote" line, Production list's "Sifariş" column, Work Order detail's order link, Finance Invoice detail's order link, and the Sales Project workspace's Material rollup table all rendered a bare UUID (or a truncated fragment) instead of the referenced record's actual name/number. Fixed with client-side lookups against each entity's existing `GET`-by-id endpoint (no backend schema changes) — the same category of gap as the prior session's Customer `assigned_manager_id` fix | Frontend — Orders, Production, Finance, Sales | Matches "remove developer wording" directly: an office employee should never see a UUID |
| **Material detail page's Prices table showed a truncated UUID under a "Currency" column header** that didn't even match what was being displayed — a real display bug, not just a translation gap. Now resolves and shows the actual price list's name and currency, reusing the same `listPriceLists()` call the sibling Price Lists page already makes | Frontend — Catalog | An employee checking a Stone's pricing across price lists saw garbled ID fragments under a mislabeled column |
| **~30 untranslated English strings on the Communication Integrations page** — every credential-form field label ("Phone Number ID", "Access Token", "SMTP Host", ...), the provider dropdown, and the Queue/Diagnostics tables' status/direction/provider/action values were raw English or raw backend slugs inside an otherwise fully Azerbaijani page. All now translated (with graceful loanwords kept where standard, e.g. "SMTP", "IMAP") | Frontend — Communication | The single largest untranslated surface found anywhere in the audit |
| **Material creation form's Material Type dropdown showed raw English options** ("Sintered Stone", "Porcelain", "Quartz", ...) inside an Azerbaijani form — translated the *displayed* labels only; the stored value is unchanged, so no data migration was needed | Frontend — Catalog | |
| Several real mistranslations fixed: Leads' `qualified` status read "Keyfiyyətli" (≈"high-quality") instead of "Uyğun" (qualified, in the sales-pipeline sense); a Sales item type read "Moydadır" (not a real word) instead of "Lavabo dolabısı" (vanity); `projectType_stairs` and `itemType_stairs` used two different Azerbaijani words for the same English concept within the same locale file; Tasks' `recurrenceInterval` was the bare English/Latin loanword "Interval" instead of "Təkrarlanma aralığı" | Frontend — CRM, Sales, Tasks | |
| Several real UX/consistency gaps closed: Tasks list table headers were plain text while every sibling list (Customers, Leads) had click-to-sort headers — now uses the same `SortableHeader` component; Tasks list was missing the "/"-to-focus-search shortcut every other CRM list page has; the Customer detail page had two note boxes (a general note and a timeline note) sharing the exact same placeholder text with no way to tell them apart; the Customer profile's "Layihələr" tile was mislabeled "via Production" when Projects are a Sales-module entity; the Dashboard's "Gecikən layihələr" section actually linked to the Orders page (renamed to "Gecikən sifarişlər" to match, since the Sales Project workspace has no path back to a specific overdue Order); the login screen's company-role badges showed a raw backend role string (`owner`/`rep`) instead of a translated label; a recurring task's "every N weeks" form gave no explanation of how Rule+Interval combine, now has a one-line hint; a Task's "Related to" link showed the generic word "Customer" instead of the actual customer's name | Frontend — CRM, Dashboard, Sales, login | |
| Dead/orphaned translation keys removed across all three locale files, closing a Version 1.1 backlog item verbatim ("Remove orphaned `customer.type` exposure"): `customerNew.type`/`customerProfile.type`/`customers.tableType`/the whole `customerType` namespace (the Customer type field has no UI anywhere and never did), `dashboard.notifications` (duplicate of `sectionNotifications`), `catalog.title`/`catalog.subtitle`/`catalog.tabSlabs` (superseded by Sprint 2's rename to "Materiallar"), `nav.quotes` (Quotes only ever existed inside the Project workspace tabs), `orders.prodStatus_unassigned`/`instStatus_unassigned` (never wired to any UI), and `tasks.relatedCustomer` (replaced by the real customer name, see above) | Frontend — i18n cleanup | |
| **Backend fix, found via live testing during the audit, not by reading code**: `finance/expenses` and `finance/invoices/[id]`'s due-date field used a free-text input with an English `"YYYY-MM-DD"` placeholder instead of a native date picker — switched both to `type="date"`. The Expense category filter's "no filter" option incorrectly reused `common.allStatuses` ("Bütün statuslar") for what is actually a category filter — added a dedicated `finance.allCategories` key | Frontend — Finance | |
| Minor accessibility/consistency fixes: the cancel-reason `<textarea>` on Orders/Production/Installation/Finance Invoice detail pages had no `aria-label` (only a visually-adjacent `<p>`, not programmatically associated) — added; the AI Dashboard's "Recent Activity" table used ad hoc sticky-header classes instead of the shared `stickyTheadClass`/`tableScrollShellClass` constants every other list table uses — swapped; the AI Dashboard's lead-analysis label and button both read "Analyze Lead" back-to-back — the label now reads plain "Lead"; the AI recommendation Edit action opens a raw JSON textarea with zero explanation — added a one-line caption; the Sales Quote builder's per-section subtotal was missing its currency code while every other money value on the page showed one — added; the Sales "Add Item" form's Notes field was missing the `sm:col-span-3` wrapper its neighboring Save button had, squeezing it into a narrow column on wide screens — fixed by wrapping in the correct grid span | Frontend — AI, Sales | |

**Explicitly deferred, not fixed in this pass** (recorded rather than silently skipped): Sales Project workspace's Photos tab shows only captions, no actual `<img>` thumbnails (a real feature gap, larger than a polish fix); Material detail page has no inline edit for its own Specifications (name/color/finish) — only sub-resources (images, documents, thickness/size options) are editable; Slabs and Price List Entry material pickers are a flat 100-row `<select>` with no search, unlike the searchable Stone selector Sales' Project Item form already has; Slabs list has no pagination; Settings hub tiles have no icons, unlike the primary sidebar; Reports' headline Revenue/Profit/Cost KPI cards show bare numbers with no currency unit (fixing this requires plumbing the active company's currency into the Reports page, out of scope for a wording/consistency pass); Installation's `layout.tsx` hand-rolls its own tab bar instead of the shared `SectionTabs` component (its active-tab-match logic isn't a drop-in swap, per the auditing fork's own caution); per-channel-type badge colors were considered and declined, since the existing `LeadChannelBadge` already establishes a single-tone-for-category-badges convention elsewhere in the same file, and the palette only has 5 tones for 6 channel types.

**Verification:** full backend suite (537/537 passing, unchanged — one narrowly-scoped fix in a prior session's file was already covered by that session's tests), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes), Node ICU spot-checked for correct Azerbaijani date output ("13 iyl 2026", "iyul 2026") before shipping, and a live smoke test against the real dev database and running backend confirming all touched routes render without a server-side crash and that the Material Prices / Price Lists API contracts match the new frontend code.

---

## Version 2.19.0 — "Load More" Pagination for Customers & Leads

**Theme:** closes the last open Medium-priority Version 1.1 item — the cursor-based pagination contract has existed server-side on `GET /crm/customers`/`GET /crm/leads` since before this roadmap was written, but neither list page ever consumed `next_cursor` or passed a `cursor` back, so both silently capped at the default `limit=25` with no way to see anything beyond it. Frontend-only, no new module, no API contract changes.

| Change | Area | Business value |
|---|---|---|
| "Load more" button added to the Customers and Leads list pages, wired to the existing `next_cursor`/`cursor` contract both endpoints already returned/accepted | Frontend — CRM | Any company past 25 customers or 25 leads previously had no way to see the rest of the list at all — this was silent data loss, not just a missing convenience |
| **Real bug found and fixed via live testing, not by reading code**: the pattern being copied from the existing Catalog Materials "Load more" button (`frontend/app/(app)/catalog/materials/page.tsx`, shipped earlier) had a stale-closure bug — its `reload` callback read a separate `cursor` state variable that was deliberately excluded from the `useCallback` dependency array (an `eslint-disable-next-line` suppressed the warning), so `reload` was never recreated when `cursor` changed. Clicking "Load more" therefore always re-fetched page one and appended it again, duplicating all 25 rows forever and never reaching page two — confirmed via a live Playwright smoke test (React duplicate-key console warnings, `rowsAfter` exactly double `rowsBefore`) against a seeded 30-row dataset, not caught by `tsc`, the build, or any existing test | Frontend — Catalog, CRM | Materials' own "Load more" button had been broken exactly this way since it shipped; fixing it only in the two new pages would have meant knowingly copying and shipping the same bug a third time |
| Fix applied uniformly to all three pages (Customers, Leads, Materials): removed the redundant `cursor` state entirely and changed `reload` to accept the cursor to fetch as an explicit argument (`reload({ append: true, cursor: nextCursor })`) instead of reading it from a closure-captured state variable, eliminating the staleness class of bug rather than papering over this one instance | Frontend — CRM, Catalog | The corrected pattern is now the one future list pages should copy, not the original |
| `common.loadMore` translation key added to all three locale files (previously only existed under the `catalog` namespace) so both new pages and any future page can share one key | Frontend — i18n | Avoids a fourth near-duplicate `loadMore` key the next list page to add pagination would otherwise need |

**Verification:** full backend suite (537/537 passing, unchanged — no backend files touched), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes), and a live Playwright smoke test against a freshly seeded dev database (30 customers, 30 leads, 30 materials, each exceeding the default page size of 25) confirming: first page loads 25 rows, "Load more" appends exactly the remaining 5 with zero duplicates, the button correctly disappears once `next_cursor` is `null`, and zero browser console errors — for all three affected pages, both before (reproducing the bug) and after the fix.

---

## Version 2.20.0 — Shareable/Persisted List Filters

**Theme:** closes the next Version 1.1 item in the roadmap's own suggested sequencing ("pagination UI and filter persistence" — pagination shipped in 2.19.0, filter persistence here). Customers and Leads filters (status/channel, search, sort) previously lived only in component state, so a manager's filtered view ("my In Production customers") couldn't be bookmarked or shared with a teammate — reloading or sending the link back always landed on the unfiltered list. Frontend-only, no new module, no API contract changes.

| Change | Area | Business value |
|---|---|---|
| New `useUrlFilters` hook (`frontend/lib/use-url-filters.ts`) — two-way sync between a list page's filter state and its URL query string via `router.replace` (not `push`, so filter tweaks don't pollute browser back-history) | Frontend — shared | A single ~35-line primitive both pages (and any future list page) can reuse, rather than each page hand-rolling `useSearchParams`/`router.replace` wiring |
| Customers (`?status=&search=&sort=&archived=1`) and Leads (`?channel=&search=&sort=`) list pages read their initial filter state from the URL on mount and write it back (debounced, via the same debounced `search` value already used for the API call) whenever the user changes a filter; default values are omitted from the query string entirely, so an unfiltered list still shows a bare `/crm/customers` URL | Frontend — CRM | A manager filtering to "In Production" customers can now copy the URL and hand it directly to a teammate, or bookmark it, and land on the exact same filtered view |
| Both pages split into a thin default-export wrapper plus an inner component wrapped in `<Suspense>`, required by Next.js App Router whenever a statically-generated page uses `useSearchParams()` (a hard build-time requirement, not a style choice — omitting it fails `next build`) | Frontend — CRM | Keeps both routes in the static-prerendered set (confirmed unchanged in the build output) rather than forcing them fully dynamic |
| URL hydration is deliberately mount-only (state seeded from the URL once, not kept in sync with back/forward browser navigation), mirroring the same SSR-safe, hydration-mismatch-avoiding pattern this codebase's existing `useLocalStorageState` hook already established — the server render and first client paint both ignore the URL, then a post-mount effect applies the real values | Frontend — CRM | Avoids a hydration-mismatch class of bug on the controlled search/status inputs; full back/forward-button filter history was explicitly out of scope for this S-sized item (the roadmap asks for bookmarkable/shareable, not full navigation history) |

**Verification:** full backend suite (537/537 passing, unchanged — no backend files touched), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes, `/crm/customers` and `/crm/leads` still statically prerendered), and a live Playwright smoke test: set a status + search filter on Customers, confirmed the URL updated to `?status=in_production&search=Ali`, navigated directly to that URL in a fresh page load and confirmed both the search box and status dropdown were pre-populated and only the matching customer rendered; cleared filters and confirmed the URL returned to the bare path; repeated the channel + search case on Leads (`?channel=instagram&search=Test`) with the same reload-and-rehydrate check — zero console errors throughout.

---

## Complexity key

- **S** (Small): a few files, one focused PR, low risk
- **M** (Medium): a small feature slice, may touch backend + frontend, moderate testing surface
- **L** (Large): a real feature with its own data model additions, multiple screens, meaningful test coverage
- **XL** (Extra Large): a new module-sized effort — new domain model, new Clean Architecture layers, new migration set

---

## Summary

| Version | Theme | Net-new entities? | Net-new modules? |
|---|---|---|---|
| 1.1 | Finish & harden what 1.0 shipped | No | No |
| 1.2 | Make the CRM a daily operating tool | Tasks/Reminders only | No |
| 2.0 | First modules beyond CRM | Yes (Stone Catalog → Sales/Quotes → Orders → Production → Installation → Finance → Reports) | Yes |
| 2.7 | Unified omnichannel communication, beyond the original ten-module list | Yes (Channel, Conversation, Message, ConversationNote, MessageTemplate) | Yes (Communication Center) |
| 2.8 | AI recommendations across CRM/Communication/Sales/Tasks — the last of the original ten modules | Yes (AIRecommendation) | Yes (AI Sales Assistant) |
| 2.8.1 | UX & platform polish (design system, dark mode, tables, forms, mobile, a11y, print) — frontend-only, no new modules | No | No |
| 2.9 | Real WhatsApp/Instagram/Messenger/Email/SMS/Webhook provider integrations for Communication Center | Yes (ChannelCredential, MessageQueueEntry, IntegrationLogEntry) | No (extends Communication Center) |
| 2.9.1 | Enterprise Polish — production audit across all ten modules: logging, query efficiency, error handling, validation, security, UI/UX consistency | No | No |
| 2.9.2 | Production Readiness & G-STONE Onboarding — Phase 3 application-wide audit ahead of real daily use: navigation, branding, CRUD completeness, filters/sort/export, permissions, company switching, i18n, dev artifacts | No | No |
| 2.9.3 | Production Readiness follow-up — i18n, dark-mode chart colors, breadcrumb consistency across ten detail pages | No | No |
| 2.10.0 | G-STONE Sprint 2 — simplified 9-section navigation, Layihə-centric project-item types, structured Material entry, 1C-territory pages regrouped into a Settings hub | No (extended `VALID_ITEM_TYPES` vocabulary only) | No |
| 2.11.0 | Measurement & Room Management — Project → Room → Project Item hierarchy with per-item Material/Measurement revisions/Drawings/Photos, all inside the existing Sales module | Yes (Room, ProjectItem, ProjectItemMeasurement, ProjectItemDrawing, ProjectItemPhoto) | No (extends Sales' existing Project workspace) |
| 2.12.0 | Production-Ready Material Selector — normalized per-Stone Thickness/Size options, a searchable Stone selector, and named supplier-brand suggestions, preparing the catalog for a future official supplier-catalog import | Yes (MaterialThickness, MaterialSize; ProjectItem gained 2 FK columns) | No (extends Catalog + Sales' existing Project workspace) |
| 2.13.0 | The Complete Project Workflow — 10-tab Project workspace (Ümumi/Məkanlar/Məmulatlar/Materiallar/Ölçülər/Çertyojlar/Fotolar/İstehsal/Quraşdırma/Təhvil), per-item completion ("Təhvil") status, expanded Room/Item type vocabulary | Yes (ProjectItem gained 1 column) | No (extends Sales' existing Project workspace) |
| 2.15.0 | Sprint 6: Operational Dashboard Redesign — replaces CRM stats counters with a daily-ops view (greeting, measurements/production/installation/overdue KPIs, task/installation/overdue/notification/inquiry sections), plus a company-scoped measurements endpoint | No | No (extends Dashboard + Sales) |
| 2.16.0 | CSV Export for Customers & Leads — filter-matching CSV downloads for both list pages, closing the last unpicked Version 1.2 item | No | No (extends CRM) |
| 2.17.0 | Assigned Manager Picker — a real company-users dropdown on the Customer form/profile (replacing a raw UUID), a backend PATCH-semantics fix enabling explicit un-assignment, and toast confirmations on the Customer detail page's remaining write actions | No | No (extends CRM) |
| 2.18.0 | Full-App UX Audit — six parallel page-by-page passes across every module: global date-locale fix, illegible dark-mode headers, duplicate cancel-button labels, silent write-action failures on Sales/Installation, raw-UUID leaks, ~30 untranslated Integrations field labels, real mistranslations, dead-key cleanup closing a Version 1.1 backlog item, and a date-input/category-filter backend-adjacent fix found via live testing | No | No (frontend-only, one narrowly-scoped fix) |
| 2.19.0 | "Load More" Pagination for Customers & Leads — closes the last open Version 1.1 Medium item; also found and fixed a real stale-closure bug in the copied "Load more" pattern (including the pre-existing Catalog Materials button it was copied from) that silently duplicated rows instead of paginating | No | No (frontend-only) |
| 2.20.0 | Shareable/Persisted List Filters — Customers/Leads filter state (status/channel, search, sort) now reflected in the URL query string via a new shared `useUrlFilters` hook, making filtered views bookmarkable and shareable between teammates | No | No (frontend-only) |

## Change log

- **2026-06-30:** Approved with one architectural change — inserted **Stone Catalog** ahead of Sales in the Version 2.0 dependency chain, since quotations require real stone brands, collections, slabs, pricing, and technical specifications. The chain is now CRM → Tasks & Reminders → Stone Catalog → Sales (Quotes) → Orders → Production → Installation → Finance → Reports. The standalone "Inventory" module from the original draft is absorbed into Stone Catalog (slab/lot tracking belongs with the catalog data it describes); Purchasing remains a later, off-critical-path module.
- **2026-06-30:** Stone Catalog module delivered as the second business module (built as a real plugin, zero core changes): Brand, Collection, Stone Material, Slab (with a 5-state lifecycle and per-company unique slab numbers), Warehouse, Price List + Entry, and Material Image/Document linking via the shared core documents pipeline. Database, API (28 endpoints), full UI (8 screens), 41 new backend tests (167/167 total passing), and documentation are all complete. Sales (Quotes) is next.
- **2026-07-01:** Sales (Quotes: Projects, Quotes, Sections, Measurements, Items, PDF export) and Orders (status workflow from an accepted Quote through production/installation/completion, financial snapshot, per-item production/installation status, audit + events) both delivered, ahead of Tasks & Reminders (1.2) — the team chose to keep momentum on the Version 2.0 module chain rather than backfill 1.2 first. Tasks & Reminders is still unbuilt.
- **2026-07-04:** Reports module delivered, out of strict dependency order — the roadmap called for Production, Installation, and Finance to exist first, but none of the three do yet (their pipeline statuses live only as fields inside Orders). Rather than block Reports on three more XL modules, Reports was scoped to what's real today: **Executive Dashboard** and **Sales Analytics** are full cross-module aggregations over CRM/Sales/Orders data; **Production Analytics** and **Installation Analytics** report on Orders' existing status workflow, per-item production/installation status, and audit-log status-change history (real cycle-time metrics, not simulated); **Finance Analytics** reports on Orders' financial snapshot (revenue/cost/profit/margin) rather than real invoicing, since no Finance module exists. All five have KPI cards, interactive charts, date-range filtering, and PDF/Excel export. Production, Installation, and Finance **as dedicated modules** (work orders + slab consumption, crew scheduling, invoicing/payments) remain unbuilt and are still next per the dependency chain below — Reports will need revisiting once they land, since today's Production/Installation/Finance analytics are a proxy over Orders, not the real thing.
  - Also fixed in passing (found while manually verifying Reports against a real migrated database, not just the test suite): the Orders module had shipped with no Alembic migration at all, so its tables never existed outside test runs; and the Sales/Orders detail pages relied on undefined CSS classes and a PDF-download link that could never actually authenticate.
- **2026-07-04:** Production module delivered as the fourth business module: WorkOrder (one per Order, gated on reaching "approved_for_production") consuming every slab-linked OrderItem via a WorkOrderItem join row, with its own shop-floor lifecycle (queued → cutting → polishing → quality_check → completed/cancelled). This closes a real gap that existed since Orders shipped: accepting a Quote reserves its linked slabs, but nothing ever moved a reserved slab to `in_production` or `sold` once an Order existed — Order creation just copied `slab_id` onto each OrderItem and stopped there. Completing a work order now sells its slabs, marks each item's production_status `done`, and advances the Order to `ready`; cancelling releases the slabs back to `available`. Order detail gained a "Create Work Order" action and a link to the existing one. New Production list/detail screens, nav entry, en/az/ru translations, 8 new backend tests (219/219 total passing), and a real Alembic migration (generated and applied against the actual dev database this time, learning from the Orders gap found the same day). Verified end-to-end through the live API and the actual UI, not just the test suite.
- **2026-07-04:** Installation module delivered as the fifth business module: InstallationJob (one per Order, gated on reaching `ready`/`delivered`) with its own lifecycle (scheduled → en_route → in_progress → completed/cancelled), new Crew/CrewMember entities (no "team of installers" concept existed anywhere before this), photo attachments and the customer signature both via the existing core Documents pipeline (a signature is just a `photo_type="signature"` row, captured with a hand-built HTML canvas), and real in-app notifications created on assignment/reschedule/status-change (no email/SMS provider or scheduler exists in this codebase, so that's the honest scope). Completing a job cascades `installation_status` onto every Order item and advances the Order to `installed`, mirroring Production's pattern exactly. Frontend: dashboard, a hand-built calendar and click-to-advance Kanban (no calendar/DnD library added), crew management, and a job detail screen with the full photo/signature/completion-report workflow. Also added `GET /api/v1/core/companies/users` — the "Assigned Manager picker" endpoint flagged as unbuilt back in the 1.1 items, needed here for crew member assignment and reused rather than duplicated. Reports' Installation Analytics was rewired onto real InstallationJob/Crew data (daily installations, delays, crew productivity, average install time) instead of the Orders-field proxy, exactly as flagged as follow-up when Reports shipped earlier the same day. 38 new backend tests (239/239 total passing). Verified end-to-end against a real migrated dev database and the actual browser UI, which caught and fixed a real bug (a UUID-vs-string serialization error in crew productivity) that the automated suite had missed because its fixture never assigned a crew — the fixture now does, closing that gap.
- **2026-07-06:** Finance module delivered as the sixth business module, completing the original Version 2.0 dependency chain: Invoice (one per Order, gated on reaching `ready`/`delivered`/`installed`/`completed`, snapshotting the Order's items into InvoiceLine rows and its `total_final` into the invoice total) with its own lifecycle (draft → sent → partially_paid/overdue → paid, cancellable from any non-terminal state), and Payment records against an invoice. `partially_paid`/`paid` are deliberately unreachable through the manual status-change endpoint — they are exclusively a side effect of recording a payment, so `amount_paid` and `status` can never drift apart. A standalone Expense entity (category, amount, optional Order link) covers the `expenses` table from `DATABASE_DESIGN.md`'s conceptual schema. Matching every other module built so far, cross-module reads (Order, Installation job) go through direct application-layer calls into Orders'/Installation's own repositories rather than the event bus — `event_subscriptions` remains empty on every module's manifest to date, including this one; only publishing (`InvoiceCreated`, `InvoiceStatusChanged`, `PaymentReceived`, `ExpenseCreated`) is wired up, per the pattern every prior module has actually followed rather than the conceptual event-subscription design in `PROJECT_ANALYSIS.md`. Order detail gained a "Create Invoice" action and a link to the existing one, mirroring the Work Order / Installation Job cards exactly. New Invoices/Expenses list + Invoice detail screens (with an inline record-payment form), nav entries, en/az/ru translations, 31 new backend tests (270/270 total passing), and a real Alembic migration (generated and applied against the actual dev database). Reports' Finance Analytics was intentionally left reading Orders' financial snapshot rather than rewired onto real Invoice/Payment data — that KPI set (recognized/pipeline/cancelled revenue by Order status) is a different question than invoice aging/collection, and conflating them was judged riskier than leaving a clearly-labeled follow-up.
- **2026-07-06:** Tasks & Reminders (1.2) delivered — the last unbuilt item from either roadmap chain, closing out both Version 1.2 and Version 2.0 in full. Built inside the CRM module (`/api/v1/crm/tasks`, `crm:tasks:read`/`write`) rather than as a new top-level module, per `API_SPECIFICATION.md`'s original placement and this document's own "no new top-level module" framing for 1.2 — the one dual-representation pattern from CRM's Phase 2 (a separate `domain/entities.py`) was deliberately not resurrected, since every module built since Catalog (5 of 6) operates on its SQLAlchemy model directly, and Tasks follows that later, dominant convention instead. Scope grew well past the original M-sized roadmap line item: full CRUD, assignee/priority/tags/due-date/status, recurring tasks (daily/weekly/monthly/yearly, with day-of-month clamping and lead-time-preserving reminders across occurrences), and in-app notifications (assigned/reassigned, plus reminder/overdue) reusing Installation's exact Notification pattern. There is still no background job scheduler anywhere in this codebase (Celery/Redis remain provisioned but unused), so reminder/overdue notifications are generated by a pull endpoint (`POST /crm/task-notifications/check`) the frontend calls on Dashboard/Tasks-page load rather than a scheduled push — the same honest constraint Installation's notifications already documented. The "Manager picker" dependency this line item originally listed as a blocker turned out to already be satisfied: Installation's `GET /api/v1/core/companies/users` (added 2026-07-04) is reused directly for the assignee picker. New Tasks list (search/filter/sort by title, status, priority, assignee, tag) + New Task + Task detail screens (status transitions, inline edit, recurring-series view, delete), two new Dashboard widgets (My Open/Overdue/Due-Today stat cards, a My Tasks list, and a Notifications card), nav + quick-create entries, en/az/ru translations, 42 new backend tests (312/312 total passing), and a real Alembic migration (generated and applied against the actual dev database). Verified end-to-end against the live API and dev database (task creation, recurrence spawning, deletion) and the actual frontend routes.
- **2026-07-06:** Communication Center (Version 2.7) delivered — a new module beyond the original ten-module list, built as its own plugin (`modules/communication`, depends on CRM/Sales/Orders) rather than folded into CRM, since its scope (five channel types, a provider abstraction, conversations, messages, templates) is module-sized, not a CRM sub-feature. Channel (a company may configure several of the same type, e.g. multiple WhatsApp Business numbers), Conversation (one thread per contact per channel, `open`/`pending`/`closed` with no terminal state), Message (inbound/outbound, with attachments via the shared core Documents pipeline), ConversationNote (internal, customer-invisible), and MessageTemplate (covers both formal templates and shortcut-triggered quick replies as one entity). **No real WhatsApp Business/Meta Graph/SMTP/SMS-gateway integration exists** — every channel type resolves to a `NullChannelProvider` that simulates a successful send, behind a `ChannelProvider` interface designed so a real integration is a provider-registry addition, not a rewrite; `POST /communication/inbound` stands in for where a real, signature-verified webhook will eventually land. Customer identification (matching a sender's phone/handle/email against an existing CRM Customer field) and **auto-Lead-creation for unknown senders** both go straight through CRM's own `CustomerModel` query and `CreateLeadUseCase` — the latter's docstring already anticipated exactly this integration point ("adding a future automated channel integration... means calling this same use-case," written when Lead capture first shipped). Conversations can link to a Customer, Lead, Project, Quote, or Order. Full CRUD, RBAC (`communication:channels/conversations/notes/templates:read|write`), multi-company isolation, audit + events on every write. Frontend: a responsive three-pane Inbox (conversation list, chat window, customer profile side panel — single-pane on mobile with back navigation), Channels and Templates management screens, nav entries, en/az/ru translations. 36 new backend tests (348/348 total passing). `API_SPECIFICATION.md` and `DATABASE_DESIGN.md` were updated in place (section 13 and 5.6 respectively) — the first time either frozen doc has been updated for a module since Stone Catalog's "as actually implemented" sections; every module shipped in between (Sales, Orders, Production, Installation, Finance, Tasks) left them stale, which is worth fixing in a follow-up pass since those docs are supposed to be the source of truth for exactly this kind of question.

Both original roadmap chains (1.1's remaining items aside) are fully delivered: CRM → Tasks & Reminders → Stone Catalog → Sales → Orders → Production → Installation → Finance → Reports. Communication Center (2.7) closes a gap outside that original sequencing entirely. Version 1.1's smaller polish items (toasts, bulk actions, upload allowlist, refresh-token revocation) and Version 1.2's non-Tasks items (configurable pipeline stages, role-scoped views, CSV export) remain the only items never picked up, having been consistently deprioritized in favor of the Version 2.0+ module chain across every session since 2026-07-01. Reports' Production Analytics still reports on Orders/audit-log data as a proxy — worth revisiting now that a real Production module exists, to decide whether it should read WorkOrder data instead (Installation Analytics already made this switch); the same question now also applies to Finance Analytics vs. real Invoice/Payment data, per the note above. `API_SPECIFICATION.md`/`DATABASE_DESIGN.md` documentation debt for Sales/Orders/Production/Installation/Finance/Tasks (see this entry) is the other open item worth a dedicated pass.
- **2026-07-06:** AI Sales Assistant (Version 2.8) delivered — the last of the original ten modules (`PROJECT_ANALYSIS.md` §9 named AI as deliberately last), built as its own plugin (`modules.ai`, depends on `crm`/`catalog`/`sales`/`orders`/`communication`, and is placed last in `INSTALLED_MODULES` accordingly). A single `AIRecommendation` entity covers all 27 recommendation types across four analysis entry points — `POST /ai/leads/{id}/analyze`, `/ai/conversations/{id}/analyze`, `/ai/quotes/{id}/analyze`, `/ai/tasks/suggest` — discriminated by `recommendation_type` rather than one table per type, the same pattern `MessageTemplate` established for templates/quick-replies. **No real LLM provider is integrated**: an `AIProvider` interface and registry name five real providers (OpenAI, Anthropic, Gemini, Ollama, Azure OpenAI) that all currently resolve to `MockAIProvider`, a genuinely deterministic heuristic engine (regex/keyword/statistical scoring against this company's own real Lead/Customer/Quote/Task/Conversation data, not a canned response) — swapping in a real provider later is a one-line registry change, no use case/schema/frontend change. **"AI never performs a business action automatically" is a structural property, not a UI promise**: `ReviewRecommendationUseCase` is the only place a recommendation's status ever changes (`pending`→`accepted`/`rejected`/`edited`, final once set), and it never imports or calls another module's write-side use cases — verified by a dedicated test that re-fetches the underlying Lead after accepting a recommendation and asserts it is byte-identical. Every recommendation stores its full audit trail (`provider`, `model`, `prompt`, `response`, `confidence_score`, `execution_time_ms` measured by the calling use case rather than self-reported, `requested_by`, `reviewed_by`/`reviewed_at`). Sales Intelligence's cross-sell/upsell/discount/margin-risk/price-anomaly signals are computed from this company's own historical *accepted* quotes (real transaction data), not a static price list. Frontend: a dedicated AI Dashboard (lead score distribution, pipeline health, win probability, at-risk customers, follow-up/daily recommendations, recent activity feed, usage statistics) plus inline "Analyze" integration points on the Leads list and the Communication Inbox, all sharing one `RecommendationCard` component (Accept/Reject/Edit, raw-JSON detail toggle) so the review experience is identical everywhere a recommendation appears. RBAC (`ai:recommendations:read/write`, `ai:dashboard:read`), multi-company isolation, en/az/ru translations, nav entry. 79 new backend tests (427/427 total passing), frontend typecheck and production build both clean. `API_SPECIFICATION.md` §14 and `DATABASE_DESIGN.md` §5.7 were updated in place alongside this entry, continuing the "as actually implemented" documentation practice Communication Center (2.7) restarted.
- **2026-07-06:** UX & Platform Polish (Version 2.8.1) delivered — a frontend-only cross-cutting pass, not a new module, once all nine modules built so far (CRM through AI) gave enough surface area to see the accumulated inconsistency: "Inter" was declared in `tailwind.config.ts` as the font but never actually loaded by any page, so the whole app had silently been rendering in the browser's default system-ui font since Phase 1. Fixed by loading Montserrat via `next/font/google` and, more importantly, converting every color token (`bg`, `surface`, `border`, `text-primary`, `text-secondary`, `primary`, `success`, `warning`, `danger`, `info`) from a hardcoded hex value in `tailwind.config.ts` to a CSS custom property, so the ~100+ existing `bg-bg`/`text-text-primary`/etc. classes across every page repoint automatically with zero per-page changes — this is what makes dark mode (a header toggle, persisted to `localStorage`, applied via an inline pre-hydration script so there's no light-flash on load) a token swap exactly as `UI_UX_GUIDELINES.md` section 3 said it would eventually be, rather than a component rewrite. Built a reusable table toolkit (`components/ui/data-table.tsx`: column resize, column visibility, sticky headers, `localStorage`-backed saved filter presets) and applied it in full to the four highest-traffic list pages (Leads, Customers, Invoices, Tasks); the other ~14 top-level list tables got a mechanical, CSS-only sticky-header + overflow-x-auto (instead of overflow-hidden, which was silently clipping columns on narrow viewports) safety pass. `Button` gained a `loading` prop (spinner + `aria-busy`), `Field` gained `aria-invalid`/`aria-describedby` and a red error border, and a new global `Toast` primitive covers success/error confirmations, wired into the Leads page as the demonstrated pattern. `AppShell`'s sidebar — previously a fixed 224px block with no mobile treatment at all — becomes a slide-over drawer with a hamburger toggle below the `lg` breakpoint (Escape/backdrop/close-button dismissible), the single highest-value mobile fix since every page depends on `AppShell`. Global `:focus-visible` ring and a skip-to-content link replace the scattered per-component focus styling. Global `@media print` rules force a light palette regardless of the active theme and hide navigation/buttons; the Invoice detail page gained a dedicated Print button as the natural print target in an ERP. Dashboard's derived task/customer/lead filtering and sorting moved into `useMemo`. **Zero backend files were touched** — verified by the full 427/427 backend suite passing unchanged — and no existing API contract changed; frontend typecheck and production build are both clean. The full table toolkit deliberately was not force-fit onto every table in the app (many are small in-page item lists inside detail views, e.g. invoice/work-order line items, where resize/visibility/saved-filters don't add value) — the toolkit itself remains available for any future list page to adopt.
- **2026-07-06:** Real Integrations (Version 2.9) delivered — the follow-through on Communication Center's (2.7) explicit placeholder: every channel type resolving to `NullChannelProvider` "by explicit design," pending real credentials. Six real `ChannelProvider` implementations shipped — `MetaWhatsAppProvider`, `MetaInstagramProvider`, `MetaMessengerProvider` (all three via the Meta Graph API's Send API shape), `SMTPEmailProvider` (send, with real file attachments fetched and attached, not just linked), `TwilioSMSProvider`, and `GenericWebhookProvider` for arbitrary partner systems — plus a standalone `ImapMailboxClient` for inbound email sync (not a `ChannelProvider`, since receiving mail is pull-based, not push). **The `ChannelProvider` interface itself is completely unchanged**: `send()`'s signature is identical to Version 2.7, and the one addition (`test_connection()`) is a concrete, non-abstract method with a trivial default every existing provider (including `NullChannelProvider`) inherits for free — additive, not breaking. A new `ChannelCredential` table (Fernet-encrypted config, one row per channel, `provider` + `health_status` + sync cursor for IMAP) is what "upgrades" a channel from Null to real; a channel with no credential row behaves exactly as before, which is the entire backward-compatibility guarantee, verified by a dedicated regression test asserting zero `IntegrationLogEntry` rows get created when no credential exists. Real signature verification: Meta's `X-Hub-Signature-256` (HMAC-SHA256), Twilio's `X-Twilio-Signature` (HMAC-SHA1, base64, signed over the full callback URL), and a matching HMAC-SHA256 scheme for the generic webhook provider — all three verified with real cryptographic tests (not just "assume it works"), and a rejected signature is logged (`signature_valid=false`) rather than silently dropped. A send failure never surfaces as a 500: the `Message` is recorded `failed` and a `MessageQueueEntry` is created for `POST /communication/queue/process` to retry (exponential backoff, permanent failure after `max_attempts`) — a pull-triggered retry queue, since this codebase still has no background job scheduler, the same honest constraint Tasks & Reminders' reminder checks already established. One `IntegrationLogEntry` table (discriminated by `direction`/`action`) backs Provider Diagnostics, Logs, and the Webhook Monitor at once, mirroring `AIRecommendation`'s single-table-per-concern pattern from Version 2.8. Inbound messages from every real provider — webhook or IMAP — flow through the *exact same* `GetOrCreateConversationUseCase`/`ReceiveInboundMessageUseCase` Communication Center already had, so CRM auto-sync (known-Customer identification, auto-Lead-creation for unknown senders) needed no new code at all. Credential secrets are never returned to the frontend unmasked (secret-looking fields show only their last 4 characters); configuring credentials is owner-tier only via the `settings:read`/`settings:write` RBAC action-suffix convention, while queue/log visibility and retry-triggering stay at the normal viewer/rep tiers since they touch no secrets. Frontend: a new Integration Management page (`/communication/integrations`) with per-channel provider configuration forms, health badges, Test Connection/Sync Now actions, a Queue Monitor, and a combined Provider Diagnostics/Webhook Monitor log view. **No real Meta/Twilio/SMTP account existed in this development environment** — every provider is verified against a mocked HTTP/SMTP/IMAP layer (real request-building and response-parsing code, just not a live third-party network call); wiring in production credentials is purely an operator action (`PUT /communication/channels/{id}/credential`), not a code change. 61 new backend tests (488/488 total passing), frontend typecheck and production build both clean, and a live smoke test against the real dev database confirmed the full flow end-to-end: configuring a webhook credential, a validly-signed inbound webhook creating a conversation + auto-created Lead, an invalidly-signed one being rejected with `403` and still logged, and both appearing correctly in the Webhook Monitor. `API_SPECIFICATION.md` §15 and `DATABASE_DESIGN.md` §5.8 were updated in place alongside this entry.
- **2026-07-07:** Enterprise Polish (Version 2.9.1) delivered — a production-readiness audit across all ten shipped modules, no new features and no business-logic or API changes. Two independent research passes (backend and frontend) surveyed every module for duplicated code, query efficiency, error handling, validation, logging, security, and UI/UX/accessibility/responsive-layout consistency. Findings and fixes are detailed in the "Version 2.9.1" section above; highlights: application-level logging added to Communication's previously-silent error paths, an N+1 query in Sales quote-acceptance's slab reservation logic batched into one query, a real silent-failure bug fixed on Orders/Production/Finance-Invoice/Installation-Job detail pages (write actions had no error handling at all — a failed request produced no feedback whatsoever), a list-page table-wrapper duplication cleanup across 8 files, and a hardened production-boot guard covering the channel-credentials encryption key alongside the existing JWT-secret check. Also completed two pieces of work an interrupted prior session had left half-finished: `crm/tasks/new`'s success toast (imported and instantiated but never called) and the `customerCreated`/`taskCreated` translation keys its and `crm/customers/new`'s `t()` calls already referenced but that didn't exist in any locale file yet. Full backend suite passing (489/489, one new test added for the credential-key boot guard), frontend typecheck and production build both clean, and a live end-to-end smoke test (login, company switch, every touched page, a real customer created through the UI) confirmed no regressions.
- **2026-07-08:** Production Readiness & G-STONE Onboarding (Version 2.9.2, "Phase 3") delivered — an application-perspective audit ahead of real daily use by G-STONE GALLERY, distinct from 2.9.1's code-quality-focused pass: this one drove the actual UI (navigation, every CRUD flow, filters/sort/export, permissions, company switching, i18n) rather than just reading source. Four independent research passes plus hands-on fixing and a live Playwright smoke test. Found and fixed one release-blocking bug missed by every prior automated-only audit: Sales Projects' "Create Project" customer picker requested `limit=200` against an endpoint capped at 100, so the picker 400'd and rendered empty on every load — Create was unusable from that form, and only surfaced by actually opening it in a browser. Also fixed: Orders' `sort` query parameter was accepted end-to-end but silently ignored by `OrderRepository.list()` (now wired to a real field map); Sales Quotes had no UI to edit VAT rate/discount/currency/validity/notes after creation despite the backend endpoint and translation keys already existing from an earlier unfinished pass (added a Quote Settings panel); a latent `discount_type` TypeScript/backend value mismatch (`"percentage"` vs. `"percent"`) that the new settings panel would have hit immediately; Catalog Brands/Warehouses had no archive/restore UI despite the backend supporting it; no favicon existed; every route shared one static browser-tab title (now updates on in-app navigation, with a documented hard-reload limitation); Sales Projects list lacked the status filter/sortable columns every sibling page had; two dead translation keys removed. A longer list of lower-severity findings (no sidebar icon library, no tablet collapsed-sidebar state, deferred pagination UI, no frontend permission-gating, unlocalized backend error messages, Production/Finance sort support, Quote Sections/Measurements in-place edit) was deliberately left for a future pass — see `PRODUCTION_READINESS_REPORT.md` for the full audit and reasoning per item. Full backend suite passing (492/492), frontend typecheck and production build both clean, and a live end-to-end Playwright smoke test (login, company selection, every touched page with zero console/HTTP errors, a real brand archived/restored, a real project → quote created with its new settings panel edited and confirmed persisted across a reload) confirmed the fixes and no regressions.
- **2026-07-08:** Production Readiness follow-up (Version 2.9.3) delivered — closing out the remaining findings from four independent research audits run alongside 2.9.2 (i18n consistency, responsive/dark-mode, navigation/breadcrumbs/icons, CRUD completeness) that hadn't yet been acted on. `az.json`'s `catalog.tableDefault` key, the one remaining untranslated string found across the whole app, was fixed (Azerbaijani is the default locale, so this was seen by every AZ user). Dashboard/Reports chart gridlines and data-point strokes, hardcoded to light-mode hex colors in `components/ui/charts.tsx`, were switched to the same `--color-border`/`--color-surface` CSS custom properties every other themed element already uses, fixing near-invisible gridlines in dark mode — verified live with the dark-mode toggle active. The breadcrumb-vs-"← Back to X" inconsistency named as far back as the 2.9.1 changelog entry (only Customers had real breadcrumbs) was closed: a shared `Breadcrumb` component was extracted from the Customers page's existing markup and applied to the other nine detail pages (Orders, Production, Catalog Materials, Catalog Price Lists, CRM Tasks, Finance Invoices, Installation Jobs, Sales Projects, and the Sales Quote builder, the last of which now shows a full three-level trail). Eight now-orphaned `backTo*` translation keys were removed from all three locale files afterward. One finding from an earlier research pass — that CRM Customer archive was missing a "Restore" button, assumed fixable by wiring the existing `PATCH /crm/customers/{id}` endpoint — was investigated and found to be based on a false premise: `UpdateCustomerUseCase` never touches `deleted_at`, and no restore use case exists anywhere in the backend, so building one would be new business logic outside this pass's scope; recorded as a genuine open gap rather than silently "fixed" against a capability that doesn't exist. A sidebar icon library was again considered and declined for the same reason as 2.9.2: a real design-system decision touching every nav entry, not a bug fix. No backend files were touched, so the full 492/492 backend suite is unchanged; frontend typecheck and production build are both clean, and a live Playwright smoke test exercised five of the ten breadcrumb-refactored detail pages with real records and confirmed the dark-mode chart fix via computed style against the live toggle.
- **2026-07-10:** Sprint 6: Operational Dashboard Redesign (Version 2.15.0) delivered — the Dashboard, unchanged since Phase 1 as a customer/lead counter screen, is rebuilt into a daily-ops worklist now that Sales/Orders/Production/Installation/Finance/Tasks all have real operational data of their own to surface. A time-of-day greeting uses the logged-in user's real first name (via the existing `me()` call) in place of the generic "Welcome back" + role line. Four new KPI cards — measurements today, work orders in production, installations scheduled tomorrow, overdue work (overdue tasks + overdue orders combined) — replace the old active/archived-customer and open/converted-lead counters. Five new sections give the page an actual worklist: Today's Tasks, Upcoming Installations, Overdue Projects (orders past their scheduled production or installation date, sorted by how overdue), Notifications (task and installation notifications merged into one feed), and Recent Inquiries (the old raw Leads table, reframed). The one new backend piece is `GET /api/v1/sales/measurements` (`ProjectItemMeasurementRepository.list_for_company`), a company-scoped, date-range-filterable measurement list — the only existing measurement endpoint was scoped to a single Project Item, which the "measurements today" KPI needed to query across the whole company. 1 new backend test; `az`/`ru`/`en` `dashboard` locale keys rewritten to match. Full backend suite passing, frontend `tsc --noEmit` clean, frontend production build clean.
- **2026-07-10:** CSV Export for Customers & Leads (Version 2.16.0) delivered — the last unpicked item from Version 1.2 (configurable pipeline stages and role-scoped views remain open; see the note above). `GET /api/v1/crm/customers/export` and `GET /api/v1/crm/leads/export` build a CSV in-process (Python's `csv` module over `io.StringIO`) from the exact same repository `list()` methods and filter parameters the read endpoints already use — status/lead-source/search/sort for customers, status/channel/search/sort for leads — so the export always matches whatever the user is currently looking at, capped at 10,000 rows. No new RBAC permission: both routes are gated by the existing `crm:customers:read`/`crm:leads:read`, matching how Reports' PDF/Excel export and Sales' Quote PDF export already reuse their sibling read permission rather than inventing an export-specific one. Output is UTF-8 with a leading BOM (`﻿`) so Excel — the realistic destination for this file — detects the encoding correctly instead of mangling Azerbaijani/Cyrillic characters under a guessed legacy codepage. Frontend: an "Export CSV" button next to the column-visibility menu on both the Customers and Leads list pages, wired through the existing `apiDownload` blob-download helper (`lib/api-client.ts`) — the identical mechanism Reports' export buttons and the Sales Quote PDF download already use, so no new download machinery was introduced. 6 new backend tests (CSV header/row shape, filter-respecting content for both status and channel filters, 401 without auth) across `test_customers_api.py`/`test_leads_api.py`. No audit-log or event-bus calls added, consistent with every other read-only export in this codebase (Reports, Sales PDF) — only write actions are audited. Full backend suite passing (535/535), frontend `tsc --noEmit` clean, frontend production build clean.
- **2026-07-13:** Assigned Manager Picker (Version 2.17.0) delivered — the highest-priority remaining Version 1.1 item, picked up after a status audit confirmed it was still genuinely unbuilt (the customer profile page rendered `customer.assigned_manager_id ?? "Unassigned"` — a raw UUID — and the creation form had no manager field at all, even though `UpdateCustomerUseCase`/`CreateCustomerUseCase` have validated `assigned_manager_id` against real company members since Phase 2). The picker itself was a small, additive frontend change — `GET /api/v1/core/companies/users` already existed, reused from Installation's crew-assignment work — but driving it end-to-end against the real dev API surfaced a real backend bug no test caught: `UpdateCustomerUseCase`'s PATCH handling read `assigned_manager_id: null` and "field omitted entirely" as the same Python `None`, so there was no way to actually un-assign a manager once one was set — the "Unassigned" option in the new dropdown would have silently no-opped. Fixed narrowly: a `clear_assigned_manager` flag on `UpdateCustomerInput`, set from `"assigned_manager_id" in payload.model_fields_set` in the router, distinguishes "not sent" from "explicitly cleared" for this one field only — every other Customer field's existing PATCH-omission semantics (name, notes, phone, etc.) are untouched, a deliberately narrow fix rather than a rewrite of the whole update path. Also closed the other half of Version 1.1's toast line item: the Customer detail page's four write actions (status change, manager change, note add, profile-notes save) previously had no `useToast` at all (only creation did, from 2.8.1) — now all four confirm via toast, matching the pattern every other module's detail page already uses. 2 new backend tests covering the unassign fix (explicit-null clears it; an unrelated field-only PATCH leaves it untouched). Full backend suite passing (537/537 — 535 prior + 2 new), frontend `tsc --noEmit` clean, frontend production build clean, and a live smoke test against the real dev database and running backend (login, company selection, customer created with a manager, unassigned, reassigned, and an unrelated PATCH confirmed not to clear it) — the PATCH bug was only caught this way, not by the automated suite.
- **2026-07-13:** Full-App UX Audit (Version 2.18.0) delivered — a from-scratch review of every visible screen from a real G-STONE office employee's perspective, split into six parallel research passes (Dashboard/CRM/nav, Catalog/Settings, the Sales Project workspace, Orders/Production/Installation, Finance/Reports, Communication/AI) that each read every page and locale key in scope rather than sampling. The single highest-impact finding: every date in the app rendered in English regardless of the active locale, because `lib/format.ts` hardcoded `toLocaleDateString("en-US", ...)` — fixed with a new `activeDateLocale()` helper reading the same `g_erp_locale` localStorage key `LocaleProvider` already persists, verified against Node's ICU output ("13 iyl 2026") before shipping; the Installation Calendar's separately-hardcoded month header and `charts.tsx`'s tooltip number formatting got the same fix. Reports' bar/trend charts showed raw English "No data for this period." on any empty date range across all five Reports pages, closed by adding the `emptyLabel` prop `StatusBarList` already had. Four detail pages (CRM Task, Finance Invoice, Order, Sales Quote builder) had a real dark-mode bug: `bg-text-primary` (near-white in dark mode) combined with `text-white` rendered illegible section headers, fixed by switching to `bg-primary`. Orders/Production/Installation/Finance Invoice detail pages each had a toggle button and a confirm button sharing the exact same label, making the cancel flow look broken on first click — the toggle now hides once the confirm card opens. The Sales Project workspace, its Quote builder, and Installation's Kanban/Crews pages — all built or extended after Version 2.9.1's silent-failure sweep — had the exact same unhandled-write-action bug that sweep fixed everywhere else; every write handler in all four surfaces now has real error feedback. Several raw-UUID leaks (Orders list, Order/Work-Order/Invoice detail links, the Material Prices table showing a truncated ID under a mismatched "Currency" header) were resolved via client-side lookups against each entity's existing `GET`-by-id endpoint, no backend schema changes. The Communication Integrations page had roughly 30 untranslated English strings (every credential-form field label, the provider dropdown, and the Queue/Diagnostics tables' raw status values) inside an otherwise fully Azerbaijani page — all translated. Real mistranslations fixed: Leads' `qualified` status read "Keyfiyyətli" (≈"high-quality") instead of "Uyğun"; a Sales item type read the non-word "Moydadır" instead of "Lavabo dolabısı"; `projectType_stairs`/`itemType_stairs` used two different Azerbaijani words for the same concept; Tasks' `recurrenceInterval` was a bare loanword. Closed a Version 1.1 backlog item verbatim ("Remove orphaned `customer.type` exposure") alongside five other dead-key groups across all three locale files. One backend-adjacent fix was made after live testing surfaced it during the audit (not from reading code): Finance's Expense/Invoice due-date fields used free-text inputs with an English placeholder instead of native date pickers, and the Expense category filter incorrectly reused the generic "all statuses" key. A dozen smaller consistency fixes are itemized in the Version 2.18.0 section above; several findings (Photos tab shows no actual images, Material detail has no inline spec editing, non-searchable Slabs/Price-List material pickers, Reports KPI currency labeling, Installation's hand-rolled tab bar, per-channel badge colors) were explicitly deferred as either out of proportion for a wording/consistency pass or in conflict with an existing app convention. No backend files were touched except via the two date-input/filter-key frontend fixes noted above (no backend code changed at all this pass — the finance fixes were frontend-only, unlike 2.17.0's genuine backend bug). Full backend suite passing (537/537, unchanged), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes), and a live smoke test against the real dev database and running backend confirming every touched route renders without a server-side crash and that the Material Prices/Price Lists API contracts match the new frontend code.
- **2026-07-14:** "Load More" Pagination for Customers & Leads (Version 2.19.0) delivered — the last open Medium-priority Version 1.1 item, picked up after confirming both `GET /crm/customers` and `GET /crm/leads` have returned a correct `{items, next_cursor}` cursor contract all along, with neither list page ever consuming it (both silently capped at the default `limit=25`, discarding `next_cursor`, with no pagination UI at all). The plan was to copy the existing "Load more" button pattern already shipped on the Catalog Materials page — but a live Playwright smoke test against a seeded 30-row dataset caught a real bug in that very pattern before it could be copied a second and third time: `reload`'s `useCallback` read a `cursor` state variable that was deliberately excluded from its own dependency array (suppressed with `eslint-disable-next-line react-hooks/exhaustive-deps`), so `reload` was never recreated when `cursor` changed — clicking "Load more" always re-fetched page one under a stale `cursor=null` closure and appended it again, duplicating all 25 rows forever and never reaching page two. This had already shipped, unnoticed, on the Materials page. Fixed at the root in all three pages (Customers, Leads, and Materials): the redundant `cursor` state was removed entirely and `reload` now takes the cursor to fetch as an explicit call-time argument (`reload({ append: true, cursor: nextCursor })`) instead of reading it from a closure-captured variable, which eliminates the whole staleness class rather than patching this one symptom. A shared `common.loadMore` translation key was added to all three locale files (previously scoped only to the `catalog` namespace) so future list pages don't need a fourth near-duplicate key. No backend files were touched (the cursor contract was already correct) — full backend suite passing (537/537, unchanged), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes), and a live Playwright smoke test against a freshly seeded dev database (30 customers, 30 leads, 30 materials) confirmed the fix on all three pages: first page loads 25 rows, "Load more" appends exactly the remaining 5 with zero duplicates and zero React key warnings, and the button correctly disappears once `next_cursor` is `null` — reproduced failing before the fix (`rowsAfter` exactly double `rowsBefore`, React duplicate-key console errors) and passing after, on the same seeded data.
- **2026-07-14:** Shareable/Persisted List Filters (Version 2.20.0) delivered — the next Version 1.1 item per the roadmap's own suggested sequencing (pagination in 2.19.0, filter persistence here). Customers (status, search, sort, archived toggle) and Leads (channel, search, sort) filter state lived only in component `useState`, so a manager's filtered view could never be bookmarked or handed to a teammate via URL — reloading, or sending the link, always landed back on the unfiltered list. A new shared hook, `useUrlFilters` (`frontend/lib/use-url-filters.ts`), two-way syncs a page's filter state with its URL query string: it reads the URL once on mount to hydrate initial filter values, then writes the (debounced) current filter values back via `router.replace` — not `push`, so adjusting a filter doesn't spam browser back-history — omitting any value that's still at its default so an unfiltered list keeps a bare URL. Both pages were split into a thin default-export wrapper plus an inner component wrapped in `<Suspense>`, which Next.js App Router requires whenever a statically-generated page calls `useSearchParams()` — this is a hard `next build` failure if skipped, not a style preference; both routes remained in the static-prerendered set afterward. URL hydration is deliberately mount-only (not synced to browser back/forward navigation), mirroring the same SSR-safe pattern this codebase's `useLocalStorageState` hook already established for saved filter presets, to avoid a hydration-mismatch class of bug on the controlled search/status inputs — full back/forward filter history was out of scope for this S-sized roadmap item, which asked for bookmarkable/shareable, not full navigation history. No backend files were touched — full backend suite passing (537/537, unchanged), frontend `tsc --noEmit` clean, frontend production build clean (all 38 routes, `/crm/customers` and `/crm/leads` still statically prerendered), and a live Playwright smoke test: setting a status + search filter on Customers updated the URL to `?status=in_production&search=Ali`, a fresh navigation directly to that URL correctly pre-populated both controls and filtered the table to the one matching customer, clearing the filters returned the URL to the bare path, and the same reload-and-rehydrate round trip was confirmed on Leads (`?channel=instagram&search=Test`) — zero console errors throughout.
